# Galaxy Vast AI Trading Platform — Python Dependencies
# Fixed: torch CPU-only (saves ~3GB in Docker)
# Fixed: aiosqlite removed (was installed but never used)

# ── Web Framework ──
fastapi==0.115.0
uvicorn[standard]==0.30.6
starlette==0.38.2
python-multipart==0.0.12

# ── Config & Validation ──
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# ── Database ──
supabase==2.9.1
asyncclient==0.0.1
httpx==0.27.2
asyncpg==0.29.0
sqlalchemy==2.0.35

# ── Cache ──
redis==5.1.1

# ── Auth ──
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
PyJWT==2.9.0

# ── ML / AI ──
numpy==1.26.4
pandas==2.2.3
scikit-learn==1.5.2
xgboost==2.1.1
shap==0.46.0
statsmodels==0.14.4
scipy==1.14.1
joblib==1.4.2

# ── RL (CPU-only — saves ~3GB vs GPU) ──
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.4.1+cpu
stable-baselines3==2.3.2
gymnasium==0.29.1

# ── Dashboard ──
streamlit==1.39.0
plotly==5.24.1

# ── Telegram Bot ──
aiogram==3.13.1

# ── Backtest / Analysis ──
ta==0.11.0
pytz==2024.2

# ── Observability ──
prometheus-client==0.21.0
structlog==24.4.0
sentry-sdk==2.16.0
opentelemetry-sdk==1.27.0
opentelemetry-instrumentation-fastapi==0.48b0

# ── Security ──
cryptography==43.0.3

# ── Utilities ──
orjson==3.10.7
python-dateutil==2.9.0.post0
requests==2.32.3

# ── Testing ──
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
pytest-cov==5.0.0
