"""\nGalaxy Vast AI Trading Platform\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\nماژول: OrderStateMachine\n\nوظیفه:\n  مدیریت چرخهٔ کامل عمر یک سفارش معاملاتی از ایجاد تا بسته‌شدن.\n  این ماژول تضمین می‌کند هیچ سفارشی بدون ردیابی باقی نماند.\n\nوضعیت‌ها (States):\n  PENDING  → SUBMITTED → OPEN → PARTIAL → FILLED\n                               ↘ CANCELLED\n                               ↘ REJECTED\n                               ↘ CLOSED (پس از FILLED)\n\nاستفاده:\n  osm = OrderStateMachine.get_instance()\n  order = osm.create(signal_id=\"sig_001\", symbol=\"EURUSD\", ...)\n  osm.transition(order.order_id, OrderEvent.SUBMITTED, ticket=123456)\n"""\n\nfrom __future__ import annotations\n\nimport logging\nimport threading\nimport uuid\nfrom dataclasses import dataclass, field\nfrom datetime import datetime, timezone\nfrom enum import Enum, auto\nfrom typing import Dict, List, Optional\n\nlogger = logging.getLogger(__name__)\n\n\n# ─────────────────────────────────────────────\n#  Enums\n# ─────────────────────────────────────────────\n\nclass OrderState(str, Enum):\n    \"\"\"وضعیت‌های ممکن یک سفارش.\"\"\"\n    PENDING    = \"PENDING\"\n    SUBMITTED  = \"SUBMITTED\"\n    OPEN       = \"OPEN\"\n    PARTIAL    = \"PARTIAL\"\n    FILLED     = \"FILLED\"\n    CLOSED     = \"CLOSED\"\n    CANCELLED  = \"CANCELLED\"\n    REJECTED   = \"REJECTED\"\n\n\nclass OrderEvent(str, Enum):\n    \"\"\"رویدادهایی که وضعیت را تغییر می‌دهند.\"\"\"\n    SUBMIT    = \"SUBMIT\"\n    ACCEPT    = \"ACCEPT\"\n    PARTIAL   = \"PARTIAL\"\n    FILL      = \"FILL\"\n    CLOSE     = \"CLOSE\"\n    CANCEL    = \"CANCEL\"\n    REJECT    = \"REJECT\"\n\n\n# ─────────────────────────────────────────────\n#  Transition Table\n# ─────────────────────────────────────────────\n\n_TRANSITIONS: Dict[tuple, OrderState] = {\n    (OrderState.PENDING,   OrderEvent.SUBMIT):  OrderState.SUBMITTED,\n    (OrderState.PENDING,   OrderEvent.CANCEL):  OrderState.CANCELLED,\n    (OrderState.SUBMITTED, OrderEvent.ACCEPT):  OrderState.OPEN,\n    (OrderState.SUBMITTED, OrderEvent.REJECT):  OrderState.REJECTED,\n    (OrderState.SUBMITTED, OrderEvent.CANCEL):  OrderState.CANCELLED,\n    (OrderState.OPEN,      OrderEvent.PARTIAL): OrderState.PARTIAL,\n    (OrderState.OPEN,      OrderEvent.FILL):    OrderState.FILLED,\n    (OrderState.OPEN,      OrderEvent.CLOSE):   OrderState.CLOSED,\n    (OrderState.OPEN,      OrderEvent.CANCEL):  OrderState.CANCELLED,\n    (OrderState.PARTIAL,   OrderEvent.FILL):    OrderState.FILLED,\n    (OrderState.PARTIAL,   OrderEvent.CLOSE):   OrderState.CLOSED,\n    (OrderState.PARTIAL,   OrderEvent.CANCEL):  OrderState.CANCELLED,\n    (OrderState.FILLED,    OrderEvent.CLOSE):   OrderState.CLOSED,\n}\n\n_TERMINAL_STATES = {OrderState.FILLED, OrderState.CLOSED,\n                    OrderState.CANCELLED, OrderState.REJECTED}\n\n\n# ─────────────────────────────────────────────\n#  Data Classes\n# ─────────────────────────────────────────────\n\n@dataclass\nclass OrderRecord:\n    \"\"\"یک سفارش با تمام اطلاعاتش.\"\"\"\n    order_id:   str\n    signal_id:  str\n    symbol:     str\n    direction:  str\n    lot_size:   float\n    sl_price:   float\n    tp_price:   float\n    state:      OrderState   = OrderState.PENDING\n    ticket:     Optional[int] = None\n    open_price: Optional[float] = None\n    close_price: Optional[float] = None\n    profit:     Optional[float] = None\n    created_at: datetime     = field(default_factory=lambda: datetime.now(timezone.utc))\n    updated_at: datetime     = field(default_factory=lambda: datetime.now(timezone.utc))\n    history:    List[dict]   = field(default_factory=list)\n\n    def to_dict(self) -> dict:\n        return {\n            \"order_id\":   self.order_id,\n            \"signal_id\":  self.signal_id,\n            \"symbol\":     self.symbol,\n            \"direction\":  self.direction,\n            \"lot_size\":   self.lot_size,\n            \"sl_price\":   self.sl_price,\n            \"tp_price\":   self.tp_price,\n            \"state\":      self.state.value,\n            \"ticket\":     self.ticket,\n            \"open_price\": self.open_price,\n            \"close_price\": self.close_price,\n            \"profit\":     self.profit,\n            \"created_at\": self.created_at.isoformat(),\n            \"updated_at\": self.updated_at.isoformat(),\n        }\n\n\n# ─────────────────────────────────────────────\n#  State Machine\n# ─────────────────────────────────────────────\n\nclass OrderStateMachine:\n    \"\"\"\n    مدیریت چرخهٔ عمر سفارش‌ها — thread-safe singleton.\n\n    مثال:\n        osm = OrderStateMachine.get_instance()\n        order = osm.create(\"sig_1\", \"EURUSD\", \"BUY\", 0.01, 1.0800, 1.0900)\n        osm.transition(order.order_id, OrderEvent.SUBMIT)\n        osm.transition(order.order_id, OrderEvent.ACCEPT, ticket=123456)\n    \"\"\"\n\n    _instance: Optional[\"OrderStateMachine\"] = None\n    _lock: threading.Lock = threading.Lock()\n\n    def __init__(self) -> None:\n        self._orders: Dict[str, OrderRecord] = {}\n        self._order_lock = threading.RLock()\n\n    @classmethod\n    def get_instance(cls) -> \"OrderStateMachine\":\n        if cls._instance is None:\n            with cls._lock:\n                if cls._instance is None:\n                    cls._instance = cls()\n        return cls._instance\n\n    def create(self, signal_id: str, symbol: str, direction: str,\n               lot_size: float, sl_price: float, tp_price: float) -> OrderRecord:\n        \"\"\"یک سفارش جدید با وضعیت PENDING می‌سازد.\"\"\"\n        order = OrderRecord(\n            order_id  = str(uuid.uuid4()),\n            signal_id = signal_id,\n            symbol    = symbol,\n            direction = direction.upper(),\n            lot_size  = lot_size,\n            sl_price  = sl_price,\n            tp_price  = tp_price,\n        )\n        with self._order_lock:\n            self._orders[order.order_id] = order\n        logger.info(\"order.created order_id=%s symbol=%s dir=%s\",\n                    order.order_id, symbol, direction)\n        return order\n\n    def get(self, order_id: str) -> Optional[OrderRecord]:\n        return self._orders.get(order_id)\n\n    def get_by_ticket(self, ticket: int) -> Optional[OrderRecord]:\n        with self._order_lock:\n            for order in self._orders.values():\n                if order.ticket == ticket:\n                    return order\n        return None\n\n    def list_active(self) -> List[OrderRecord]:\n        with self._order_lock:\n            return [o for o in self._orders.values()\n                    if o.state not in _TERMINAL_STATES]\n\n    def list_all(self) -> List[OrderRecord]:\n        with self._order_lock:\n            return list(self._orders.values())\n\n    def transition(self, order_id: str, event: OrderEvent, *,\n                   ticket: Optional[int] = None,\n                   open_price: Optional[float] = None,\n                   close_price: Optional[float] = None,\n                   profit: Optional[float] = None) -> OrderRecord:\n        \"\"\"وضعیت سفارش را بر اساس رویداد تغییر می‌دهد.\"\"\"\n        with self._order_lock:\n            order = self._orders.get(order_id)\n            if order is None:\n                raise KeyError(f\"order not found: {order_id}\")\n            key = (order.state, event)\n            next_state = _TRANSITIONS.get(key)\n            if next_state is None:\n                raise ValueError(\n                    f\"invalid transition: {order.state.value} + {event.value}\"\n                )\n            old_state = order.state\n            order.state      = next_state\n            order.updated_at = datetime.now(timezone.utc)\n            if ticket is not None:     order.ticket = ticket\n            if open_price is not None: order.open_price = open_price\n            if close_price is not None: order.close_price = close_price\n            if profit is not None:     order.profit = profit\n            order.history.append({\n                \"from\": old_state.value, \"event\": event.value,\n                \"to\": next_state.value, \"ts\": order.updated_at.isoformat(),\n            })\n        logger.info(\"order.transition order_id=%s %s -[%s]-> %s\",\n                    order_id, old_state.value, event.value, next_state.value)\n        return order\n\n    def purge_closed(self, keep_last: int = 500) -> int:\n        with self._order_lock:\n            closed = sorted(\n                [o for o in self._orders.values() if o.state in _TERMINAL_STATES],\n                key=lambda o: o.updated_at,\n            )\n            to_remove = closed[:-keep_last] if len(closed) > keep_last else []\n            for order in to_remove:\n                del self._orders[order.order_id]\n        return len(to_remove)\n\n    def stats(self) -> dict:\n        with self._order_lock:\n            all_orders = list(self._orders.values())\n        counts = {s.value: sum(1 for o in all_orders if o.state == s) for s in OrderState}\n        return {\"total\": len(all_orders), \"by_state\": counts}\n