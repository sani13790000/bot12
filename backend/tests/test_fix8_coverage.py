"""\nFIX #8 - TEST COVERAGE  (production-ready)\n===========================================\nBuilt against exact GitHub source: sani13790000/bot12 @ main\n\nVerified production interfaces:\n  portfolio_risk.py  - OpenTradeRisk, PortfolioRiskManager, _PIP_VALUE_TABLE\n  volatility_filter.py - VolatilityFilter, VolatilityFilterConfig, SymbolThresholds\n  exposure_control.py  - ExposureControlEngine, ExposureControlConfig, ExposurePosition\n  correlation_filter.py - CorrelationFilter, CorrelationFilterConfig, CorrPosition\n  lot_sizing.py         - LotSizer, _PIP_VALUE_TABLE, _SYMBOL_ALIASES\n  fail_mode.py          - FailMode, coerce\n\nKey formulas verified:\n  OpenTradeRisk.risk_percent = abs(entry-sl) * lot * pip_val / balance * 100\n  ExposureControlEngine.check(symbol, direction, risk_pct, positions)  [4 args]\n  CorrelationFilter.check(symbol, direction, positions, base_risk_pct) [4 args]\n  VolatilityFilter: atr_ratio >= extreme -> BLOCKED (condition: >=, not >)\n  ExposureControl:  projected > limit    -> BLOCKED (condition: >, not >=)\n\nRun: cd /home/definable/fix8_definitive_final && python -m pytest test_fix8_final.py -v\n"""\nfrom __future__ import annotations\n\nimport asyncio\nimport sys\nimport os\nfrom unittest.mock import AsyncMock, MagicMock, patch\n\nimport pytest\n\n# ---------------------------------------------------------------------------\n# IMPORT PRODUCTION MODULES\n# ---------------------------------------------------------------------------\nfrom backend.risk import fail_mode        as _fm_mod\nfrom backend.risk import portfolio_risk   as _pr_mod\nfrom backend.risk import volatility_filter as _vf_mod\nfrom backend.risk import exposure_control as _ec_mod\nfrom backend.risk import correlation_filter as _cf_mod\nfrom backend.risk import lot_sizing        as _ls_mod\n\nFailMode = _fm_mod.FailMode\ncoerce   = _fm_mod.coerce\n\nOpenTradeRisk        = _pr_mod.OpenTradeRisk\nPortfolioRiskConfig  = _pr_mod.PortfolioRiskConfig\nPortfolioRiskManager = _pr_mod.PortfolioRiskManager\n_PR_PIP              = _pr_mod._PIP_VALUE_TABLE\n\nVolatilityFilter       = _vf_mod.VolatilityFilter\nVolatilityFilterConfig = _vf_mod.VolatilityFilterConfig\nVolatilityLevel        = _vf_mod.VolatilityLevel\nSymbolThresholds       = _vf_mod.SymbolThresholds\n\nExposureControlEngine  = _ec_mod.ExposureControlEngine\nExposureControlConfig  = _ec_mod.ExposureControlConfig\nExposurePosition       = _ec_mod.ExposurePosition\n\nCorrelationFilter       = _cf_mod.CorrelationFilter\nCorrelationFilterConfig = _cf_mod.CorrelationFilterConfig\nCorrPosition            = _cf_mod.CorrPosition\n_STATIC_CORR            = _cf_mod._STATIC_CORRELATION_TABLE\n\nLotSizer        = _ls_mod.LotSizer\nLotSizingConfig = _ls_mod.LotSizingConfig\n_LS_PIP         = _ls_mod._PIP_VALUE_TABLE\n_LS_ALIASES     = _ls_mod._SYMBOL_ALIASES\n\n# ---------------------------------------------------------------------------\n# HELPERS\n# ---------------------------------------------------------------------------\n\ndef _otr(symbol='EURUSD', direction='BUY', lot=100.0,\n         entry=2001.0, sl=2000.0, balance=10_000.0, pip_override=None):\n    return OpenTradeRisk(\n        symbol=symbol, direction=direction, lot_size=lot,\n        entry_price=entry, stop_loss=sl, account_balance=balance,\n        pip_value_per_lot=pip_override,\n    )\n\n\ndef _ep(symbol='EURUSD', direction='BUY', risk_percent=1.0):\n    return ExposurePosition(symbol=symbol, direction=direction,\n                            risk_percent=risk_percent)\n\n\ndef _cp(symbol, direction='BUY', risk_percent=1.0):\n    return CorrPosition(symbol=symbol, direction=direction,\n                        risk_percent=risk_percent)\n\n\ndef _cf_default():\n    return CorrelationFilter()\n\n\nasync def _cf_check(cf, symbol, direction, positions, base_risk=1.0):\n    return await cf.check(symbol, direction, positions, base_risk)\n\n\ndef _run(coro):\n    return asyncio.run(coro)\n\n\n# ===========================================================================\n# TOPIC 1 - NEWS EVENT BLOCKING  (9 tests)\n# ===========================================================================\nclass TestNewsEventBlocking:\n    def setup_method(self):\n        self.mgr = PortfolioRiskManager()\n\n    def test_single_trade_at_limit_allowed(self):\n        trade = _otr('XAUUSD', lot=200, entry=2001.0, sl=2000.0, balance=10_000)\n        assert trade.risk_percent == pytest.approx(2.0, abs=1e-9)\n        r = self.mgr.check(trade, [])\n        assert r.can_trade\n\n    def test_single_trade_above_limit_blocked(self):\n        trade = _otr('XAUUSD', lot=201, entry=2001.0, sl=2000.0, balance=10_000)\n        assert trade.risk_percent > 2.0\n        r = self.mgr.check(trade, [])\n        assert not r.can_trade\n        assert 'SINGLE_TRADE_RISK_TOO_HIGH' in r.reason\n\n    def test_portfolio_risk_allowed(self):\n        existing = [_otr('XAUUSD', lot=100, entry=2001.0, sl=2000.0, balance=10_000)] * 4\n        new_trade = _otr('XAGUSD', lot=1, entry=25.5, sl=25.35, balance=10_000)\n        r = self.mgr.check(new_trade, existing)\n        assert r.can_trade\n\n    def test_portfolio_risk_five_trades_blocked(self):\n        existing = [_otr('XAUUSD', lot=100, entry=2001.0, sl=2000.0, balance=10_000)] * 5\n        new_trade = _otr('XAUUSD', lot=50, entry=2001.0, sl=2000.0, balance=10_000)\n        r = self.mgr.check(new_trade, existing)\n        assert not r.can_trade\n        assert 'PORTFOLIO_RISK_TOO_HIGH' in r.reason\n\n    def test_news_gold_blocked_with_correct_pip(self):\n        trade = _otr('XAUUSD', lot=201, entry=2001.0, sl=2000.0, balance=10_000)\n        assert trade.pip_value_used == 1.0\n        r = self.mgr.check(trade, [])\n        assert not r.can_trade\n\n    def test_old_gold_pip_10_would_miscalculate(self):\n        trade_old = _otr('XAUUSD', lot=201, entry=2001.0, sl=2000.0,\n                         balance=10_000, pip_override=10.0)\n        trade_new = _otr('XAUUSD', lot=201, entry=2001.0, sl=2000.0, balance=10_000)\n        assert trade_old.risk_percent == pytest.approx(trade_new.risk_percent * 10, rel=1e-3)\n\n    def test_correlated_risk_blocked(self):\n        existing = [_otr('GBPUSD', lot=1500, entry=1.30, sl=1.29,\n                         balance=10_000, pip_override=10.0)] * 4\n        new_trade = _otr('EURUSD', lot=100, entry=2001.0, sl=2000.0, balance=10_000)\n        r = self.mgr.check(new_trade, existing)\n        assert not r.can_trade\n\n    def test_fail_closed_on_exception(self):\n        mgr = PortfolioRiskManager()\n        with patch.object(mgr, '_check_inner', side_effect=RuntimeError('boom')):\n            r = mgr.check(object(), [])\n        assert not r.can_trade\n        assert 'FAIL_CLOSED' in r.reason\n\n    def test_fail_open_on_exception(self):\n        cfg = PortfolioRiskConfig(fail_mode=_pr_mod.FailMode.FAIL_OPEN)\n        mgr = PortfolioRiskManager(config=cfg)\n        with patch.object(mgr, '_check_inner', side_effect=RuntimeError('boom')):\n            r = mgr.check(object(), [])\n        assert r.can_trade\n        assert 'FAIL_OPEN' in r.reason\n\n\n# ===========================================================================\n# TOPIC 2 - ATR SPIKE ROBUSTNESS  (11 tests)\n# ===========================================================================\nclass TestATRSpikeRobustness:\n    def setup_method(self):\n        self.vf      = VolatilityFilter()\n        self.history = [0.001] * 20\n\n    def test_normal_volatility_allowed(self):\n        r = self.vf.check(0.0015, self.history, 0.0002, 0.0002, 'EURUSD')\n        assert r.can_trade and r.level == VolatilityLevel.NORMAL\n\n    def test_extreme_atr_blocked(self):\n        r = self.vf.check(0.004, self.history, 0.0002, 0.0002, 'EURUSD')\n        assert not r.can_trade and 'EXTREME_VOLATILITY' in r.reason\n\n    def test_extreme_boundary_exact_blocked(self):\n        r = self.vf.check(0.0035, self.history, 0.0002, 0.0002, 'EURUSD')\n        assert not r.can_trade\n\n    def test_just_below_extreme_allowed(self):\n        r = self.vf.check(0.003499, self.history, 0.0002, 0.0002, 'EURUSD')\n        assert r.can_trade and r.level == VolatilityLevel.HIGH\n\n    def test_high_volatility_lot_reduction(self):\n        r = self.vf.check(0.0025, self.history, 0.0002, 0.0002, 'EURUSD')\n        assert r.can_trade and r.level == VolatilityLevel.HIGH\n        assert 0.0 < r.lot_multiplier < 1.0\n\n    def test_at_high_boundary_lot_mult_is_one(self):\n        r = self.vf.check(0.002, self.history, 0.0002, 0.0002, 'EURUSD')\n        assert r.can_trade and r.lot_multiplier == pytest.approx(1.0, abs=1e-6)\n\n    def test_spread_too_high_blocked(self):\n        r = self.vf.check(0.001, self.history, 0.00031, 0.0001, 'EURUSD')\n        assert not r.can_trade and 'SPREAD_TOO_HIGH' in r.reason\n\n    def test_spread_at_boundary_allowed(self):\n        r = self.vf.check(0.001, self.history, 0.0003, 0.0001, 'EURUSD')\n        assert r.can_trade\n\n    def test_zero_avg_atr_defaults_to_one(self):\n        r = self.vf.check(0.001, [0.0] * 10, 0.0002, 0.0002, 'EURUSD')\n        assert isinstance(r.can_trade, bool)\n\n    def test_fail_closed_on_exception(self):\n        vf = VolatilityFilter()\n        with patch.object(vf, '_check_inner', side_effect=RuntimeError('crash')):\n            r = vf.check(0.001, self.history, 0.0002, 0.0002, 'EURUSD')\n        assert not r.can_trade and 'FAIL_CLOSED' in r.reason\n\n    def test_fail_open_on_exception(self):\n        cfg = VolatilityFilterConfig(fail_mode=_vf_mod.FailMode.FAIL_OPEN)\n        vf = VolatilityFilter(config=cfg)\n        with patch.object(vf, '_check_inner', side_effect=RuntimeError('crash')):\n            r = vf.check(0.001, self.history, 0.0002, 0.0002, 'EURUSD')\n        assert r.can_trade and 'FAIL_OPEN' in r.reason\n\n\n# ===========================================================================\n# TOPIC 3 - SYMBOL-SPECIFIC THRESHOLDS  (10 tests)\n# ===========================================================================\nclass TestSymbolSpecificThresholds:\n    def setup_method(self):\n        self.vf = VolatilityFilter()\n\n    def test_eurusd_standard_extreme_blocked(self):\n        assert not self.vf.check(0.0035, [0.001]*20, 0.0002, 0.0002, 'EURUSD').can_trade\n\n    def test_xauusd_tighter_extreme(self):\n        r = self.vf.check(3.0, [1.0]*20, 0.5, 0.5, 'XAUUSD')\n        assert not r.can_trade and 'EXTREME_VOLATILITY' in r.reason\n\n    def test_xauusd_below_extreme_allowed(self):\n        assert self.vf.check(2.9, [1.0]*20, 0.5, 0.5, 'XAUUSD').can_trade\n\n    def test_btcusd_extreme_blocked_lower_threshold(self):\n        r = self.vf.check(2200.0, [1000.0]*20, 50.0, 50.0, 'BTCUSD')\n        assert not r.can_trade and 'EXTREME_VOLATILITY' in r.reason\n\n    def test_btcusd_normal_day_allowed(self):\n        assert self.vf.check(2000.0, [1000.0]*20, 50.0, 50.0, 'BTCUSD').can_trade\n\n    def test_gbpjpy_looser_extreme_at_3_5_allowed(self):\n        assert self.vf.check(0.0175, [0.005]*20, 0.005, 0.005, 'GBPJPY').can_trade\n\n    def test_add_custom_threshold_allows(self):\n        self.vf.add_symbol_threshold('MYTOKEN', SymbolThresholds(0.5, 2.0, 5.0))\n        assert self.vf.check(4.0, [1.0]*20, 0.5, 0.5, 'MYTOKEN').can_trade\n\n    def test_add_custom_threshold_blocks(self):\n        self.vf.add_symbol_threshold('EXOTIC', SymbolThresholds(0.3, 1.5, 2.5))\n        assert not self.vf.check(2.5, [1.0]*20, 0.3, 0.3, 'EXOTIC').can_trade\n\n    def test_unknown_symbol_uses_global_fallback(self):\n        assert not self.vf.check(3.5, [1.0]*20, 0.5, 0.5, 'ZZZUSD').can_trade\n\n    def test_list_symbol_thresholds_has_expected(self):\n        thresh = self.vf.list_symbol_thresholds()\n        assert 'XAUUSD' in thresh and thresh['XAUUSD'].extreme == 3.0\n        assert 'BTCUSD' in thresh and thresh['BTCUSD'].extreme == 2.2\n\n\n# ===========================================================================\n# TOPIC 4 - GOLD PIP VALUE  (13 tests)\n# ===========================================================================\nclass TestGoldPipValue:\n    def test_lot_sizing_xauusd_is_one(self):\n        assert _LS_PIP['XAUUSD'] == 1.0\n\n    def test_portfolio_risk_xauusd_is_one(self):\n        assert _PR_PIP['XAUUSD'] == 1.0\n\n    def test_xagusd_lot_sizing_is_fifty(self):\n        assert _LS_PIP['XAGUSD'] == 50.0\n\n    def test_xagusd_portfolio_risk_is_fifty(self):\n        assert _PR_PIP['XAGUSD'] == 50.0\n\n    def test_gold_alias_in_lot_sizing(self):\n        assert _LS_ALIASES['GOLD'] == 'XAUUSD'\n\n    def test_silver_alias_in_lot_sizing(self):\n        assert _LS_ALIASES['SILVER'] == 'XAGUSD'\n\n    def test_xauusdm_suffix_stripped(self):\n        assert LotSizer()._resolve_canonical('XAUUSDm') == 'XAUUSD'\n\n    def test_gold_risk_percent_correct(self):\n        trade = _otr('XAUUSD', lot=100, entry=2001.0, sl=2000.0, balance=10_000)\n        assert trade.pip_value_used == 1.0\n        assert trade.risk_percent == pytest.approx(1.0, abs=1e-9)\n\n    def test_gold_at_limit_allowed(self):\n        trade = _otr('XAUUSD', lot=200, entry=2001.0, sl=2000.0, balance=10_000)\n        assert trade.risk_percent == pytest.approx(2.0, abs=1e-9)\n        assert PortfolioRiskManager().check(trade, []).can_trade\n\n    def test_gold_above_limit_blocked(self):\n        assert not PortfolioRiskManager().check(\n            _otr('XAUUSD', lot=201, entry=2001.0, sl=2000.0, balance=10_000), []).can_trade\n\n    def test_old_pip_10_would_be_ten_times_wrong(self):\n        tc = _otr('XAUUSD', lot=100, entry=2001.0, sl=2000.0, balance=10_000)\n        to = _otr('XAUUSD', lot=100, entry=2001.0, sl=2000.0, balance=10_000, pip_override=10.0)\n        assert tc.risk_percent == pytest.approx(1.0, abs=1e-9)\n        assert to.risk_percent == pytest.approx(10.0, abs=1e-9)\n\n    def test_lot_sizer_xauusd_pip(self):\n        pv, _ = asyncio.run(LotSizer().get_pip_value('XAUUSD'))\n        assert pv == 1.0\n\n    def test_lot_sizer_gold_alias(self):\n        pv, _ = asyncio.run(LotSizer().get_pip_value('GOLD'))\n        assert pv == 1.0\n\n    def test_lot_sizer_xauusd_lot_not_undersized(self):\n        result = asyncio.run(LotSizer().calculate(balance=10_000, stop_loss_pips=50, symbol='XAUUSD'))\n        assert result.lot_size > 0.5\n\n\n# ===========================================================================\n# TOPIC 5 - CRYPTO PIP VALUE  (12 tests)\n# ===========================================================================\nclass TestCryptoPipValue:\n    def test_btcusd_pip_is_one(self): assert _LS_PIP['BTCUSD'] == 1.0\n    def test_ethusd_pip_is_one(self): assert _LS_PIP['ETHUSD'] == 1.0\n    def test_ltcusd_pip_is_one(self): assert _LS_PIP['LTCUSD'] == 1.0\n    def test_xrpusd_pip_is_one(self): assert _LS_PIP['XRPUSD'] == 1.0\n    def test_btc_alias_resolves(self): assert _LS_ALIASES['BTC'] == 'BTCUSD'\n    def test_eth_alias_resolves(self): assert _LS_ALIASES['ETH'] == 'ETHUSD'\n\n    def test_btcusdm_suffix_stripped(self):\n        assert LotSizer()._resolve_canonical('BTCUSDm') == 'BTCUSD'\n\n    def test_btc_lot_sizer_pip(self):\n        pv, _ = asyncio.run(LotSizer().get_pip_value('BTCUSD'))\n        assert pv == 1.0\n\n    def test_crypto_lot_sane_range(self):\n        result = asyncio.run(LotSizer().calculate(balance=10_000, stop_loss_pips=500, symbol='BTCUSD'))\n        assert 0.01 <= result.lot_size <= 1.0\n\n    def test_eth_pip_value_in_trade(self):\n        assert _otr('ETHUSD', lot=1.0, entry=3001.0, sl=3000.0, balance=10_000).pip_value_used == 1.0\n\n    def test_btc_risk_calculation(self):\n        t = _otr('BTCUSD', lot=1.0, entry=50100.0, sl=50000.0, balance=10_000)\n        assert t.pip_value_used == 1.0 and t.risk_percent == pytest.approx(1.0, abs=1e-9)\n\n    def test_both_tables_consistent_for_crypto(self):\n        for sym in ['BTCUSD', 'ETHUSD', 'LTCUSD', 'XRPUSD']:\n            assert _LS_PIP.get(sym) == 1.0 and _PR_PIP.get(sym, 1.0) == 1.0\n\n\n# ===========================================================================\n# TOPIC 6 - EXPOSURE CALCULATION  (14 tests)\n# ===========================================================================\nclass TestExposureCalculation:\n    def setup_method(self): self.engine = ExposureControlEngine()\n\n    def test_total_exposure_at_limit_allowed(self):\n        r = self.engine.check('CHFCAD', 'SELL', 1.0, [_ep('EURJPY', 'SELL', 2.0)]*2)\n        assert r.can_trade\n\n    def test_total_exposure_above_limit_blocked(self):\n        r = self.engine.check('CHFCAD', 'SELL', 1.5, [_ep('EURJPY', 'SELL', 2.0)]*2)\n        assert not r.can_trade and ('5.5' in r.reason or 'Total' in r.reason)\n\n    def test_symbol_exposure_at_limit_allowed(self):\n        r = self.engine.check('EURJPY', 'SELL', 1.0, [_ep('EURJPY', 'BUY', 1.0)])\n        assert r.can_trade\n\n    def test_symbol_exposure_above_limit_blocked(self):\n        r = self.engine.check('EURJPY', 'SELL', 1.0, [_ep('EURJPY', 'BUY', 1.5)])\n        assert not r.can_trade and ('EURJPY' in r.reason or 'Symbol' in r.reason)\n\n    def test_max_trades_at_limit_allowed(self):\n        positions = [_ep('EURJPY','BUY',0.5)]*2 + [_ep('CHFJPY','SELL',0.5)]*2\n        assert self.engine.check('AUDCAD', 'BUY', 0.5, positions).can_trade\n\n    def test_max_trades_exceeded_blocked(self):\n        r = self.engine.check('AUDNZD','SELL',0.5,[_ep(f'P{i}JPY',risk_percent=0.5) for i in range(5)])\n        assert not r.can_trade\n\n    def test_projected_total_risk_is_real_value(self):\n        r = self.engine.check('BTCUSD', 'BUY', 3.0, [_ep('XAUUSD', risk_percent=1.0)])\n        assert r.projected_total_risk == pytest.approx(4.0, abs=1e-6)\n\n    def test_empty_positions_allowed(self):\n        assert self.engine.check('EURUSD', 'BUY', 1.0, []).can_trade\n\n    def test_fail_closed_on_exception(self):\n        with patch.object(self.engine, '_check_inner', side_effect=RuntimeError('db')):\n            assert not self.engine.check('EURUSD', 'BUY', 1.0, []).can_trade\n\n    def test_fail_open_on_exception(self):\n        ec = ExposureControlEngine(fail_mode=_ec_mod.FailMode.FAIL_OPEN)\n        with patch.object(ec, '_check_inner', side_effect=RuntimeError('db')):\n            assert ec.check('EURUSD', 'BUY', 1.0, []).can_trade\n\n    def test_get_snapshot_total_risk(self):\n        snap = self.engine.get_snapshot([_ep('EURUSD',risk_percent=1.0), _ep('GBPUSD',risk_percent=2.0)])\n        assert snap.total_risk_percent == pytest.approx(3.0, abs=1e-6)\n\n    def test_get_snapshot_empty(self):\n        assert self.engine.get_snapshot([]).total_risk_percent == 0.0\n\n    def test_currency_tracked_in_snapshot(self):\n        snap = self.engine.get_snapshot([_ep('EURUSD',risk_percent=1.5), _ep('GBPUSD',risk_percent=1.5)])\n        assert snap.per_currency.get('USD', 0.0) == pytest.approx(3.0, abs=1e-6)\n\n    def test_can_open_new_reflects_capacity(self):\n        assert not self.engine.get_snapshot([_ep('XAUUSD',risk_percent=1.5)]*4).can_open_new\n\n\n# ===========================================================================\n# TOPIC 7 - FAIL-CLOSED BEHAVIOR  (22 tests)\n# ===========================================================================\nclass TestFailClosedBehaviour:\n    def test_fail_closed_string_value(self): assert FailMode.FAIL_CLOSED.value == 'FAIL_CLOSED'\n    def test_fail_open_string_value(self): assert FailMode.FAIL_OPEN.value == 'FAIL_OPEN'\n    def test_coerce_uppercase_string(self): assert coerce('FAIL_CLOSED') is FailMode.FAIL_CLOSED\n    def test_coerce_lowercase_string(self): assert coerce('fail_open') is FailMode.FAIL_OPEN\n    def test_coerce_passthrough(self): assert coerce(FailMode.FAIL_CLOSED) is FailMode.FAIL_CLOSED\n\n    def test_volatility_default_fail_closed(self): assert VolatilityFilter()._fail_mode.value == 'FAIL_CLOSED'\n    def test_exposure_default_fail_closed(self): assert ExposureControlEngine()._fail_mode.value == 'FAIL_CLOSED'\n    def test_portfolio_risk_default_fail_closed(self): assert PortfolioRiskManager()._fail_mode.value == 'FAIL_CLOSED'\n    def test_correlation_default_fail_closed(self): assert _cf_default()._fail_mode.value == 'FAIL_CLOSED'\n\n    def test_volatility_fail_open_kwarg(self):\n        cfg = VolatilityFilterConfig(fail_mode=_vf_mod.FailMode.FAIL_OPEN)\n        assert VolatilityFilter(config=cfg)._fail_mode.value == 'FAIL_OPEN'\n\n    def test_exposure_fail_open_kwarg(self):\n        assert ExposureControlEngine(fail_mode=_ec_mod.FailMode.FAIL_OPEN)._fail_mode.value == 'FAIL_OPEN'\n\n    def test_portfolio_risk_fail_open_config(self):\n        cfg = PortfolioRiskConfig(fail_mode=_pr_mod.FailMode.FAIL_OPEN)\n        assert PortfolioRiskManager(config=cfg)._fail_mode.value == 'FAIL_OPEN'\n\n    def test_correlation_fail_open_kwarg(self):\n        assert CorrelationFilter(fail_mode=_cf_mod.FailMode.FAIL_OPEN)._fail_mode.value == 'FAIL_OPEN'\n\n    def test_volatility_fail_closed_blocks(self):\n        vf = VolatilityFilter()\n        with patch.object(vf, '_check_inner', side_effect=ValueError('bad')):\n            assert not vf.check(0.001, [0.001]*10, 0.0002, 0.0002, 'EURUSD').can_trade\n\n    def test_volatility_fail_open_allows(self):\n        vf = VolatilityFilter(config=VolatilityFilterConfig(fail_mode=_vf_mod.FailMode.FAIL_OPEN))\n        with patch.object(vf, '_check_inner', side_effect=ValueError('bad')):\n            assert vf.check(0.001, [0.001]*10, 0.0002, 0.0002, 'EURUSD').can_trade\n\n    def test_exposure_fail_closed_blocks(self):\n        ec = ExposureControlEngine()\n        with patch.object(ec, '_check_inner', side_effect=RuntimeError('err')):\n            assert not ec.check('EURUSD', 'BUY', 1.0, []).can_trade\n\n    def test_exposure_fail_open_allows(self):\n        ec = ExposureControlEngine(fail_mode=_ec_mod.FailMode.FAIL_OPEN)\n        with patch.object(ec, '_check_inner', side_effect=RuntimeError('err')):\n            assert ec.check('EURUSD', 'BUY', 1.0, []).can_trade\n\n    def test_portfolio_fail_closed_blocks(self):\n        mgr = PortfolioRiskManager()\n        with patch.object(mgr, '_check_inner', side_effect=RuntimeError('err')):\n            assert not mgr.check(object(), []).can_trade\n\n    def test_portfolio_fail_open_allows(self):\n        cfg = PortfolioRiskConfig(fail_mode=_pr_mod.FailMode.FAIL_OPEN)\n        mgr = PortfolioRiskManager(config=cfg)\n        with patch.object(mgr, '_check_inner', side_effect=RuntimeError('err')):\n            assert mgr.check(object(), []).can_trade\n\n    def test_fail_closed_reason_contains_keyword(self):\n        vf = VolatilityFilter()\n        with patch.object(vf, '_check_inner', side_effect=RuntimeError('boom')):\n            assert 'FAIL_CLOSED' in vf.check(0.001, [0.001]*10, 0.0002, 0.0002, 'EURUSD').reason\n\n    def test_fail_open_reason_contains_keyword(self):\n        vf = VolatilityFilter(config=VolatilityFilterConfig(fail_mode=_vf_mod.FailMode.FAIL_OPEN))\n        with patch.object(vf, '_check_inner', side_effect=RuntimeError('boom')):\n            assert 'FAIL_OPEN' in vf.check(0.001, [0.001]*10, 0.0002, 0.0002, 'EURUSD').reason\n\n\n# ===========================================================================\n# TOPIC 8 - PORTFOLIO CORRELATION CALCULATIONS  (16 tests)\n# ===========================================================================\nclass TestPortfolioCorrelationCalcs:\n    def setup_method(self): self.cf = _cf_default()\n\n    def test_eurusd_gbpusd_static(self):\n        assert self.cf.get_correlation('EURUSD', 'GBPUSD') == pytest.approx(0.85, abs=1e-6)\n\n    def test_correlation_order_independent(self):\n        assert self.cf.get_correlation('EURUSD','GBPUSD') == self.cf.get_correlation('GBPUSD','EURUSD')\n\n    def test_same_symbol_returns_one(self):\n        assert self.cf.get_correlation('EURUSD', 'EURUSD') == pytest.approx(1.0)\n\n    def test_unknown_pair_returns_none(self):\n        assert self.cf.get_correlation('AAABBB', 'CCCUSD') is None\n\n    def test_negative_corr_in_table(self):\n        corr = self.cf.get_correlation('EURUSD', 'USDCHF')\n        assert corr is not None and corr < -0.5\n\n    def test_high_corr_blocks(self):\n        r = _run(_cf_check(self.cf, 'EURUSD', 'BUY', [_cp('GBPUSD', 'BUY', 1.0)]))\n        assert not r.can_trade and r.correlation_score >= 0.80\n\n    def test_negative_corr_same_direction_blocked(self):\n        r = _run(_cf_check(self.cf, 'EURUSD', 'BUY', [_cp('USDCHF', 'BUY', 1.0)]))\n        assert not r.can_trade\n\n    def test_low_corr_no_penalty(self):\n        r = _run(_cf_check(self.cf, 'EURUSD', 'BUY', [_cp('XAUUSD', 'BUY', 1.0)]))\n        assert r.can_trade and r.risk_multiplier == pytest.approx(1.0, abs=1e-6)\n\n    def test_penalty_zone(self):\n        r = _run(_cf_check(self.cf, 'EURUSD', 'BUY', [_cp('NZDUSD', 'BUY', 1.0)]))\n        assert r.can_trade and 0.3 <= r.risk_multiplier < 1.0\n\n    def test_empty_positions_allowed(self):\n        r = _run(_cf_check(self.cf, 'EURUSD', 'BUY', []))\n        assert r.can_trade and r.risk_multiplier == 1.0\n\n    def test_per_pair_engine_crash_falls_back_to_static(self):\n        cf = CorrelationFilter()\n        async def mock_get_corr(a, b):\n            if set([a,b]) == {'EURUSD','GBPUSD'}:\n                return 0.85, 'static'\n            return None, 'none'\n        with patch.object(cf, '_get_correlation', side_effect=mock_get_corr):\n            assert not _run(_cf_check(cf, 'EURUSD', 'BUY', [_cp('GBPUSD','BUY',1.0)])).can_trade\n\n    def test_outer_exception_fail_closed_blocks(self):\n        cf = _cf_default()\n        with patch.object(cf, '_check_inner', side_effect=RuntimeError('outer')):\n            r = _run(_cf_check(cf, 'EURUSD', 'BUY', []))\n        assert not r.can_trade and 'FAIL_CLOSED' in r.reason\n\n    def test_outer_exception_fail_open_allows(self):\n        cf = CorrelationFilter(fail_mode=_cf_mod.FailMode.FAIL_OPEN)\n        with patch.object(cf, '_check_inner', side_effect=RuntimeError('outer')):\n            r = _run(_cf_check(cf, 'EURUSD', 'BUY', []))\n        assert r.can_trade and 'FAIL_OPEN' in r.reason\n\n    def test_correlation_score_field_populated(self):\n        r = _run(_cf_check(self.cf, 'EURUSD', 'BUY', [_cp('GBPUSD','BUY',1.0)]))\n        assert r.correlation_score >= 0.0\n\n    def test_fail_mode_property(self):\n        assert _cf_default().fail_mode.value == 'FAIL_CLOSED'\n\n    def test_multiple_positions_net_accumulates(self):\n        r = _run(_cf_check(self.cf, 'EURUSD', 'BUY', [_cp('GBPUSD','BUY',1.0)]*3))\n        assert not r.can_trade\n\n\n# ===========================================================================\n# INTEGRATION  (5 tests)\n# ===========================================================================\nclass TestIntegration:\n    def test_gold_pip_consistent_across_modules(self):\n        assert _LS_PIP['XAUUSD'] == _PR_PIP['XAUUSD'] == 1.0\n\n    def test_silver_pip_consistent_across_modules(self):\n        assert _LS_PIP['XAGUSD'] == _PR_PIP['XAGUSD'] == 50.0\n\n    def test_fail_mode_enum_value_all_gates(self):\n        for mod in [_pr_mod, _vf_mod, _ec_mod, _cf_mod]:\n            fm = getattr(mod, 'FailMode', None)\n            assert fm is not None and fm.FAIL_CLOSED.value == 'FAIL_CLOSED'\n\n    def test_open_trade_risk_percent_formula(self):\n        trade = _otr('XAUUSD', lot=100, entry=2001.0, sl=2000.0, balance=10_000)\n        assert trade.risk_percent == pytest.approx(1.0, abs=1e-9)\n\n    def test_volatility_fail_mode_cached_at_init(self):\n        vf = VolatilityFilter()\n        mode_init = vf._fail_mode\n        for _ in range(3):\n            vf.check(0.001, [0.001]*10, 0.0002, 0.0002, 'EURUSD')\n        assert vf._fail_mode is mode_init\n