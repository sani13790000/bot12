"""\nbackend/tests/test_fix8_coverage.py\n====================================\nFIX #8 — Test Coverage  |  98 tests  |  8 topics\n\nLoader: registers backend.risk.* in sys.modules from production path\nso every 'from backend.risk.X import Y' inside production code works.\n\nNo pytest-asyncio — async tests use asyncio.run().\n"""\nfrom __future__ import annotations\n\nimport asyncio\nimport importlib.util\nimport logging\nimport math\nimport sys\nimport types\nimport unittest\nfrom unittest.mock import MagicMock, patch\n\n# ──────────────────────────────────────────────────────────────────────────────\n# Bootstrap\n# ──────────────────────────────────────────────────────────────────────────────\n_BASE = \"/tmp/fix8_final\"\n\ndef _bootstrap():\n    if \"backend\" in sys.modules:\n        return\n    bp  = types.ModuleType(\"backend\");      bp.__path__  = [_BASE]\n    brp = types.ModuleType(\"backend.risk\"); brp.__path__ = [_BASE]\n    sys.modules[\"backend\"] = bp;  sys.modules[\"backend.risk\"] = brp\n    for name in [\"fail_mode\",\"volatility_filter\",\"portfolio_risk\",\n                 \"lot_sizing\",\"correlation_filter\",\"exposure_control\"]:\n        fqn  = f\"backend.risk.{name}\"\n        spec = importlib.util.spec_from_file_location(fqn, f\"{_BASE}/{name}.py\")\n        mod  = importlib.util.module_from_spec(spec)\n        mod.__package__ = \"backend.risk\"\n        sys.modules[fqn] = mod\n        spec.loader.exec_module(mod)\n        setattr(brp, name, mod)\n\n_bootstrap()\n\n# ──────────────────────────────────────────────────────────────────────────────\n# Production imports\n# ──────────────────────────────────────────────────────────────────────────────\n_fm  = sys.modules[\"backend.risk.fail_mode\"]\n_vf  = sys.modules[\"backend.risk.volatility_filter\"]\n_pr  = sys.modules[\"backend.risk.portfolio_risk\"]\n_ls  = sys.modules[\"backend.risk.lot_sizing\"]\n_cf  = sys.modules[\"backend.risk.correlation_filter\"]\n_ec  = sys.modules[\"backend.risk.exposure_control\"]\n\nFailMode                = _fm.FailMode\ncoerce                  = _fm.coerce\n\nVolatilityFilter        = _vf.VolatilityFilter\nVolatilityFilterConfig  = _vf.VolatilityFilterConfig\nVolatilityCheckResult   = _vf.VolatilityCheckResult\nSymbolThresholds        = _vf.SymbolThresholds\n\nPortfolioRiskManager    = _pr.PortfolioRiskManager\nPortfolioRiskConfig     = _pr.PortfolioRiskConfig\nOpenTradeRisk           = _pr.OpenTradeRisk\nTradeDirection          = _pr.TradeDirection\n\nLotSizer                = _ls.LotSizer\nLotSizingConfig         = _ls.LotSizingConfig\nLotSizingMethod         = _ls.LotSizingMethod\n\nCorrelationFilter       = _cf.CorrelationFilter\nCorrelationFilterConfig = _cf.CorrelationFilterConfig\n\nExposureControlEngine   = _ec.ExposureControlEngine\nExposureControlConfig   = _ec.ExposureControlConfig\nExposurePosition        = _ec.ExposurePosition\n\n\n# ──────────────────────────────────────────────────────────────────────────────\n# Helpers\n# ──────────────────────────────────────────────────────────────────────────────\ndef _trade(symbol=\"EURUSD\", lot=1.0, entry=1.2000, sl=1.1000, bal=10_000.0):\n    return OpenTradeRisk(\n        symbol=symbol, direction=TradeDirection.BUY,\n        lot_size=lot, entry_price=entry, stop_loss=sl, account_balance=bal,\n    )\n\ndef _pos(symbol=\"EURUSD\", direction=\"BUY\", risk_pct=1.0, risk_usd=100.0):\n    return ExposurePosition(symbol=symbol, direction=direction,\n                            risk_percent=risk_pct, risk_usd=risk_usd)\n\ndef _history(n=20, val=0.001):\n    return [val] * n\n\n\n# ======================================================================\n# TOPIC 1 — News Event Blocking (9 tests)\n# ======================================================================\nclass TestNewsEventBlocking(unittest.TestCase):\n    \"\"\"\n    Issue: XAUUSD pip_value=10.0 before FIX #4 (10x wrong).\n    Patch: portfolio_risk._PIP_VALUE_TABLE[\"XAUUSD\"] = 1.0\n    Risk: 10x under-reported risk -> gates never trigger in NFP.\n    Compat: _get_pip_value() signature unchanged.\n    \"\"\"\n    def setUp(self):\n        self.mgr = PortfolioRiskManager(PortfolioRiskConfig(\n            max_portfolio_risk_pct=6.0, max_single_symbol_pct=2.0))\n    def _rp(self, t): return t.risk_percent\n    def test_single_trade_over_2pct_blocked(self):\n        t = _trade(\"EURUSD\", lot=21.0, entry=2.0, sl=1.0)\n        self.assertGreater(self._rp(t), 2.0)\n        r = self.mgr.check(t, [])\n        self.assertFalse(r.can_trade); self.assertIn(\"SINGLE_TRADE_RISK\", r.reason)\n    def test_single_trade_at_2pct_allowed(self):\n        t = _trade(\"EURUSD\", lot=20.0, entry=2.0, sl=1.0)\n        self.assertAlmostEqual(self._rp(t), 2.0, places=4)\n        self.assertTrue(self.mgr.check(t, []).can_trade)\n    def test_portfolio_total_blocked(self):\n        existing = [_trade(\"EURUSD\",lot=21.0,entry=2.0,sl=1.0),\n                    _trade(\"GBPUSD\",lot=21.0,entry=2.0,sl=1.0),\n                    _trade(\"AUDUSD\",lot=21.0,entry=2.0,sl=1.0)]\n        new = _trade(\"NZDUSD\",lot=7.0,entry=2.0,sl=1.0)\n        r = self.mgr.check(new, existing)\n        self.assertFalse(r.can_trade); self.assertIn(\"PORTFOLIO_RISK\", r.reason)\n    def test_remaining_cap_reported(self):\n        existing = [_trade(\"EURUSD\",lot=7.0,entry=2.0,sl=1.0)]\n        new = _trade(\"GBPUSD\",lot=1.0,entry=1.3010,sl=1.3000)\n        r = self.mgr.check(new, existing)\n        self.assertAlmostEqual(r.remaining_cap, max(0.0,6.0-self._rp(existing[0])), places=4)\n    def test_no_existing_trades_allowed(self):\n        self.assertTrue(self.mgr.check(_trade(\"EURUSD\",lot=1.0,entry=1.1010,sl=1.1000),[]).can_trade)\n    def test_gold_1pct_risk_correct(self):\n        t = _trade(\"XAUUSD\",lot=2.0,entry=2050.0,sl=2000.0)\n        self.assertAlmostEqual(self._rp(t), 1.0, places=4)\n        self.assertTrue(self.mgr.check(t,[]).can_trade)\n    def test_gold_overleveraged_blocked(self):\n        t = _trade(\"XAUUSD\",lot=250.0,entry=2050.0,sl=2000.0)\n        self.assertFalse(self.mgr.check(t,[]).can_trade)\n    def test_fail_closed_exception_blocks(self):\n        mgr = PortfolioRiskManager(fail_mode=FailMode.FAIL_CLOSED)\n        with patch.object(type(mgr),'_check_inner',side_effect=RuntimeError):\n            self.assertFalse(mgr.check(_trade(),[]).can_trade)\n    def test_fail_open_exception_allows(self):\n        mgr = PortfolioRiskManager(fail_mode=FailMode.FAIL_OPEN)\n        with patch.object(type(mgr),'_check_inner',side_effect=RuntimeError):\n            self.assertTrue(mgr.check(_trade(),[]).can_trade)\n\n\n# ======================================================================\n# TOPIC 2 — ATR Spike Robustness (11 tests)\n# ======================================================================\nclass TestATRSpikeRobustness(unittest.TestCase):\n    \"\"\"\n    Issue: VolatilityFilter had no try/except before FIX #6.\n    Patch: check() wraps _check_inner() with try/except + FailMode.\n    Risk: Silent crash -> trade during 4x ATR spike -> SL 4x wider than sized.\n    Compat: check() signature unchanged.\n    \"\"\"\n    def setUp(self):\n        self.vf = VolatilityFilter(); self.h = _history()\n    def test_normal_atr_allowed(self):\n        r = self.vf.check(0.001, self.h, 0.0001, 0.0001, \"EURUSD\")\n        self.assertTrue(r.can_trade); self.assertAlmostEqual(r.atr_ratio, 1.0)\n    def test_high_atr_allowed_reduced_lot(self):\n        r = self.vf.check(0.0021, self.h, 0.0001, 0.0001, \"EURUSD\")\n        self.assertTrue(r.can_trade); self.assertIn(\"HIGH_VOLATILITY\", r.reason)\n        self.assertLess(r.lot_multiplier, 1.0)\n    def test_extreme_atr_blocked(self):\n        r = self.vf.check(0.004, self.h, 0.0001, 0.0001, \"EURUSD\")\n        self.assertFalse(r.can_trade); self.assertIn(\"EXTREME_VOLATILITY\", r.reason)\n    def test_extreme_threshold_exact_blocked(self):\n        self.assertFalse(self.vf.check(0.0035, self.h, 0.0001, 0.0001, \"EURUSD\").can_trade)\n    def test_just_below_extreme_allowed(self):\n        self.assertTrue(self.vf.check(0.003499, self.h, 0.0001, 0.0001, \"EURUSD\").can_trade)\n    def test_spread_too_wide_blocked(self):\n        r = self.vf.check(0.001, self.h, 0.0003, 0.0001, \"EURUSD\")\n        self.assertFalse(r.can_trade); self.assertIn(\"SPREAD\", r.reason)\n    def test_empty_history_safe_fallback(self):\n        self.assertTrue(self.vf.check(0.001, [], 0.0001, 0.0001, \"EURUSD\").can_trade)\n    def test_zero_avg_atr_safe(self):\n        self.assertTrue(self.vf.check(0.001, [0.0]*20, 0.0001, 0.0001, \"EURUSD\").can_trade)\n    def test_fail_closed_blocks_on_exception(self):\n        vf = VolatilityFilter(VolatilityFilterConfig(fail_mode=FailMode.FAIL_CLOSED))\n        with patch.object(vf,'_check_inner',side_effect=ZeroDivisionError):\n            r = vf.check(0.001, self.h, 0.0001, 0.0001, \"X\")\n        self.assertFalse(r.can_trade); self.assertIn(\"FAIL_CLOSED\", r.reason)\n    def test_fail_open_allows_on_exception(self):\n        vf = VolatilityFilter(VolatilityFilterConfig(fail_mode=FailMode.FAIL_OPEN))\n        with patch.object(vf,'_check_inner',side_effect=RuntimeError):\n            r = vf.check(0.001, self.h, 0.0001, 0.0001, \"X\")\n        self.assertTrue(r.can_trade); self.assertIn(\"FAIL_OPEN\", r.reason)\n    def test_atr_ratio_in_result(self):\n        r = self.vf.check(0.002, self.h, 0.0001, 0.0001, \"EURUSD\")\n        self.assertAlmostEqual(r.atr_ratio, 2.0, places=4)\n\n\n# ======================================================================\n# TOPIC 3 — Symbol-Specific Thresholds (7 tests)\n# ======================================================================\nclass TestSymbolSpecificThresholds(unittest.TestCase):\n    \"\"\"\n    Issue: One global ATR threshold wrong for BTC vs Gold.\n    Patch: VolatilityFilterConfig.symbol_thresholds Dict[str, SymbolThresholds].\n    Risk: Wrong threshold -> Gold blocked normally / BTC not blocked in crash.\n    Compat: symbol_thresholds=None (default) uses global thresholds.\n    \"\"\"\n    def test_gold_tight_threshold_blocks_spike(self):\n        cfg = VolatilityFilterConfig(symbol_thresholds={\"XAUUSD\": SymbolThresholds(0.5,1.5,2.0)})\n        r = VolatilityFilter(cfg).check(2.5, [1.0]*20, 0.5, 0.5, \"XAUUSD\")\n        self.assertFalse(r.can_trade); self.assertIn(\"EXTREME_VOLATILITY\", r.reason)\n    def test_gold_normal_volatility_allowed(self):\n        cfg = VolatilityFilterConfig(symbol_thresholds={\"XAUUSD\": SymbolThresholds(0.5,1.5,2.0)})\n        self.assertTrue(VolatilityFilter(cfg).check(1.3,[1.0]*20,0.5,0.5,\"XAUUSD\").can_trade)\n    def test_crypto_loose_threshold_allows_spike(self):\n        cfg = VolatilityFilterConfig(symbol_thresholds={\"BTCUSD\": SymbolThresholds(0.3,5.0,12.0)})\n        r = VolatilityFilter(cfg).check(800.0,[100.0]*20,50.0,50.0,\"BTCUSD\")\n        self.assertTrue(r.can_trade)\n    def test_crypto_extreme_spike_blocked(self):\n        cfg = VolatilityFilterConfig(symbol_thresholds={\"BTCUSD\": SymbolThresholds(0.3,5.0,12.0)})\n        self.assertFalse(VolatilityFilter(cfg).check(1300.0,[100.0]*20,50.0,50.0,\"BTCUSD\").can_trade)\n    def test_unknown_symbol_uses_global_threshold(self):\n        cfg = VolatilityFilterConfig(symbol_thresholds={\"XAUUSD\": SymbolThresholds(0.5,1.5,2.0)},\n                                     extreme_volatility_threshold=3.5)\n        r = VolatilityFilter(cfg).check(3.0,[1.0]*20,0.5,0.5,\"EURUSD\")\n        self.assertTrue(r.can_trade)  # 3.0 < 3.5 global\n    def test_per_symbol_cache_isolated(self):\n        vf = VolatilityFilter(); h = [0.001]*20\n        vf.check(0.004, h, 0.0001, 0.0001, \"EURUSD\")\n        self.assertTrue(vf.check(0.001, h, 0.0001, 0.0001, \"GBPUSD\").can_trade)\n    def test_symbol_thresholds_fields(self):\n        st = SymbolThresholds(low=0.5, high=2.0, extreme=4.0)\n        self.assertEqual(st.low, 0.5); self.assertEqual(st.high, 2.0); self.assertEqual(st.extreme, 4.0)\n\n\n# ======================================================================\n# TOPIC 4 — Gold Pip Value (11 tests)\n# ======================================================================\nclass TestGoldPipValue(unittest.TestCase):\n    \"\"\"\n    Issue: XAUUSD pip_value=10.0 in portfolio_risk and lot_sizing before FIX #4.\n    Patch: both set to 1.0 (Gold pip=$0.01/oz x 100oz=$1.00 per pip).\n    Risk: 10x undersized lots -> actual risk 10% of intended -> limits meaningless.\n    Compat: _get_pip_value()/_resolve_pip_value() API unchanged.\n    \"\"\"\n    def test_pr_xauusd_is_1(self): self.assertEqual(_pr._PIP_VALUE_TABLE[\"XAUUSD\"], 1.0)\n    def test_pr_xagusd_is_50(self): self.assertEqual(_pr._PIP_VALUE_TABLE[\"XAGUSD\"], 50.0)\n    def test_pr_gold_alias(self):\n        val, _ = _pr._get_pip_value_with_source(\"GOLD\"); self.assertEqual(val, 1.0)\n    def test_pr_xauusdm_broker_suffix(self):\n        val, _ = _pr._get_pip_value_with_source(\"XAUUSDm\"); self.assertEqual(val, 1.0)\n    def test_ls_xauusd_is_1(self): self.assertEqual(_ls._PIP_VALUE_TABLE[\"XAUUSD\"], 1.0)\n    def test_ls_gold_alias(self): self.assertEqual(_ls._resolve_pip_value(\"GOLD\"), 1.0)\n    def test_ls_xauusdm_suffix(self): self.assertEqual(_ls._resolve_pip_value(\"XAUUSDm\"), 1.0)\n    def test_gold_trade_risk_1pct(self):\n        t = _trade(\"XAUUSD\",lot=2.0,entry=2050.0,sl=2000.0,bal=10_000)\n        self.assertAlmostEqual(t.risk_percent, 1.0, places=4)\n    def test_gold_risk_linear_with_lot(self):\n        t1 = _trade(\"XAUUSD\",lot=1.0,entry=2050.0,sl=2000.0)\n        t2 = _trade(\"XAUUSD\",lot=2.0,entry=2050.0,sl=2000.0)\n        self.assertAlmostEqual(t2.risk_percent, t1.risk_percent*2, places=4)\n    def test_lot_sizer_gold_1pct(self):\n        s = LotSizer(LotSizingConfig(method=LotSizingMethod.FIXED_PERCENT,\n                                     risk_percent=1.0,min_lot=0.01,max_lot=100.0))\n        r = asyncio.run(s.calculate(\"XAUUSD\", 10_000, 50))\n        self.assertAlmostEqual(r.lot_size, 2.0, places=2)\n    def test_lot_sizer_get_pip_value_gold(self):\n        self.assertEqual(LotSizer().get_pip_value(\"XAUUSD\"), 1.0)\n\n\n# ======================================================================\n# TOPIC 5 — Crypto Pip Value (11 tests)\n# ======================================================================\nclass TestCryptoPipValue(unittest.TestCase):\n    \"\"\"\n    Issue: Crypto pip_values wrong before FIX #4.\n    Patch: BTC/ETH/LTC/BNB/XRP all = 1.0 in lot_sizing._PIP_VALUE_TABLE.\n    Risk: Wrong value -> lot 100x over/under-sized -> blow-up on first trade.\n    Compat: _resolve_pip_value() and alias table API unchanged.\n    \"\"\"\n    def test_btcusd(self):      self.assertEqual(_ls._PIP_VALUE_TABLE[\"BTCUSD\"], 1.0)\n    def test_ethusd(self):      self.assertEqual(_ls._PIP_VALUE_TABLE[\"ETHUSD\"], 1.0)\n    def test_ltcusd(self):      self.assertEqual(_ls._PIP_VALUE_TABLE[\"LTCUSD\"], 1.0)\n    def test_bnbusd(self):      self.assertEqual(_ls._PIP_VALUE_TABLE[\"BNBUSD\"], 1.0)\n    def test_xrpusd(self):      self.assertEqual(_ls._PIP_VALUE_TABLE[\"XRPUSD\"], 1.0)\n    def test_btc_alias(self):   self.assertEqual(_ls._resolve_pip_value(\"BTC\"), 1.0)\n    def test_bitcoin_alias(self): self.assertEqual(_ls._resolve_pip_value(\"BITCOIN\"), 1.0)\n    def test_eth_alias(self):   self.assertEqual(_ls._resolve_pip_value(\"ETH\"), 1.0)\n    def test_btcusdm_suffix(self): self.assertEqual(_ls._resolve_pip_value(\"BTCUSDm\"), 1.0)\n    def test_lot_sizer_btc(self):\n        s = LotSizer(LotSizingConfig(method=LotSizingMethod.FIXED_PERCENT,\n                                     risk_percent=1.0,min_lot=0.01,max_lot=100.0))\n        r = asyncio.run(s.calculate(\"BTCUSD\", 10_000, 500))\n        self.assertAlmostEqual(r.lot_size, 0.20, places=2)\n    def test_lot_sizer_eth(self):\n        s = LotSizer(LotSizingConfig(method=LotSizingMethod.FIXED_PERCENT,\n                                     risk_percent=1.0,min_lot=0.01,max_lot=100.0))\n        r = asyncio.run(s.calculate(\"ETHUSD\", 10_000, 100))\n        self.assertAlmostEqual(r.lot_size, 1.0, places=2)\n\n\n# ======================================================================\n# TOPIC 6 — Exposure Calculation (13 tests)\n# ======================================================================\nclass TestExposureCalculation(unittest.TestCase):\n    \"\"\"\n    Issue: ExposureControlEngine.check() had NO try/except before FIX #6.\n    Patch: _check_inner() extracted; check() wraps with try/except + FailMode.\n    Risk: Uncaught AttributeError from corrupt position -> gate bypass.\n    Compat: check(symbol,direction,risk_pct,positions,balance) signature unchanged.\n    \"\"\"\n    def setUp(self):\n        self.cfg = ExposureControlConfig(max_total_exposure_percent=5.0,\n            max_per_symbol_percent=2.0, max_simultaneous_trades=3,\n            max_per_currency_percent=99.0)  # disable currency gate for unit tests\n        self.eng = ExposureControlEngine(config=self.cfg)\n    def _check(self, sym, risk, ops, bal=10_000.0, direction=\"BUY\"):\n        return self.eng.check(sym, direction, risk, ops, bal)\n    def test_no_positions_allowed(self):\n        r = self._check(\"EURUSD\", 1.0, [])\n        self.assertTrue(r.can_trade); self.assertEqual(r.reason, \"EXPOSURE_OK total=1.0%\")\n    def test_projected_risk_correct(self):\n        r = self._check(\"EURUSD\", 1.0, [_pos(\"GBPUSD\",\"BUY\",1.5)])\n        self.assertAlmostEqual(r.projected_total_risk, 2.5)\n    def test_total_exposure_exceeded_blocked(self):\n        r = self._check(\"EURUSD\", 1.0, [_pos(\"GBPUSD\",\"BUY\",4.5,450.0)])\n        self.assertFalse(r.can_trade); self.assertIn(\"MAX_EXPOSURE\", r.reason)\n    def test_total_exposure_at_limit_allowed(self):\n        ops = [_pos(\"GBPAUD\",\"BUY\",3.5,350.0)]\n        r = self.eng.check(\"EURAUD\",\"BUY\",1.5,ops,10_000)\n        self.assertTrue(r.can_trade)\n    def test_different_symbol_not_constrained(self):\n        ops = [_pos(\"EURUSD\",\"BUY\",1.5)]\n        self.assertTrue(self._check(\"GBPUSD\",1.0,ops).can_trade)\n    def test_max_simultaneous_trades_blocked(self):\n        ops = [_pos(\"EURUSD\"),_pos(\"GBPUSD\"),_pos(\"AUDUSD\")]\n        r = self._check(\"NZDUSD\",0.5,ops)\n        self.assertFalse(r.can_trade); self.assertIn(\"MAX_TRADES\", r.reason)\n    def test_duplicate_same_symbol_dir_blocked(self):\n        ops = [_pos(\"EURUSD\",\"BUY\",1.0)]\n        r = self.eng.check(\"EURUSD\",\"BUY\",1.0,ops,10_000)\n        self.assertFalse(r.can_trade); self.assertIn(\"DUPLICATE\", r.reason)\n    def test_snapshot_in_result(self):\n        ops = [_pos(\"EURUSD\",\"BUY\",1.5,150.0)]\n        r = self._check(\"GBPUSD\",0.5,ops)\n        self.assertIsNotNone(r.snapshot)\n        self.assertAlmostEqual(r.snapshot.total_risk_percent, 1.5)\n    def test_fail_closed_blocks(self):\n        eng = ExposureControlEngine(fail_mode=FailMode.FAIL_CLOSED)\n        with patch.object(eng,'_check_inner',side_effect=AttributeError):\n            r = eng.check(\"X\",\"BUY\",1.0,[],10_000)\n        self.assertFalse(r.can_trade); self.assertIn(\"FAIL_CLOSED\", r.reason)\n    def test_fail_open_allows(self):\n        eng = ExposureControlEngine(fail_mode=FailMode.FAIL_OPEN)\n        with patch.object(eng,'_check_inner',side_effect=RuntimeError):\n            r = eng.check(\"X\",\"BUY\",1.0,[],10_000)\n        self.assertTrue(r.can_trade); self.assertIn(\"FAIL_OPEN\", r.reason)\n    def test_exception_logged_fail_closed(self):\n        eng = ExposureControlEngine(fail_mode=FailMode.FAIL_CLOSED)\n        with patch.object(eng,'_check_inner',side_effect=RuntimeError(\"boom\")):\n            with self.assertLogs(\"risk.exposure_control\", level=logging.ERROR):\n                eng.check(\"X\",\"BUY\",1.0,[],10_000)\n    def test_exception_logged_fail_open(self):\n        eng = ExposureControlEngine(fail_mode=FailMode.FAIL_OPEN)\n        with patch.object(eng,'_check_inner',side_effect=RuntimeError(\"boom\")):\n            with self.assertLogs(\"risk.exposure_control\", level=logging.ERROR):\n                eng.check(\"X\",\"BUY\",1.0,[],10_000)\n    def test_snapshot_open_trade_count(self):\n        ops = [_pos(\"EURUSD\",\"BUY\",1.0),_pos(\"GBPUSD\",\"BUY\",1.0)]\n        r = self._check(\"AUDUSD\",0.5,ops)\n        self.assertEqual(r.snapshot.open_trades, 2)\n\n\n# ======================================================================\n# TOPIC 7 — Fail-Closed Behavior (22 tests)\n# ======================================================================\nclass TestFailClosedBehavior(unittest.TestCase):\n    \"\"\"\n    Issue: Multiple gates had silent allow on exception before FIX #6.\n    Patch: canonical FailMode in fail_mode.py; all gates wrap _check_inner();\n           every exception logged CRITICAL; FAIL_CLOSED blocks; FAIL_OPEN allows+logs.\n    Risk: Silent exception+allow -> unbounded exposure during system faults.\n    Compat: all constructors accept fail_mode=None (default FAIL_CLOSED).\n    \"\"\"\n    def test_fc_value(self): self.assertEqual(FailMode.FAIL_CLOSED.value, \"FAIL_CLOSED\")\n    def test_fo_value(self): self.assertEqual(FailMode.FAIL_OPEN.value, \"FAIL_OPEN\")\n    def test_coerce_upper(self):\n        self.assertIs(coerce(\"FAIL_CLOSED\"), FailMode.FAIL_CLOSED)\n        self.assertIs(coerce(\"FAIL_OPEN\"), FailMode.FAIL_OPEN)\n    def test_coerce_lower(self): self.assertIs(coerce(\"fail_open\"), FailMode.FAIL_OPEN)\n    def test_coerce_passthrough(self): self.assertIs(coerce(FailMode.FAIL_CLOSED), FailMode.FAIL_CLOSED)\n    def test_sst_vf(self): self.assertIs(_vf.FailMode, _fm.FailMode)\n    def test_sst_ec(self): self.assertIs(_ec.FailMode, _fm.FailMode)\n    def test_sst_cf(self): self.assertIs(_cf.FailMode, _fm.FailMode)\n    def test_sst_pr(self): self.assertIs(_pr.FailMode, _fm.FailMode)\n    def test_vf_default_fc(self): self.assertIs(VolatilityFilter()._fail_mode, FailMode.FAIL_CLOSED)\n    def test_ec_default_fc(self): self.assertIs(ExposureControlEngine()._fail_mode, FailMode.FAIL_CLOSED)\n    def test_cf_default_fc(self): self.assertIs(CorrelationFilter()._fail_mode, FailMode.FAIL_CLOSED)\n    def test_pr_default_fc(self): self.assertIs(PortfolioRiskManager()._fail_mode, FailMode.FAIL_CLOSED)\n    def test_vf_fc_blocks(self):\n        vf = VolatilityFilter(VolatilityFilterConfig(fail_mode=FailMode.FAIL_CLOSED))\n        with patch.object(vf,'_check_inner',side_effect=RuntimeError):\n            self.assertFalse(vf.check(0.001,_history(),0.0001,0.0001,\"X\").can_trade)\n    def test_vf_fo_allows(self):\n        vf = VolatilityFilter(VolatilityFilterConfig(fail_mode=FailMode.FAIL_OPEN))\n        with patch.object(vf,'_check_inner',side_effect=RuntimeError):\n            self.assertTrue(vf.check(0.001,_history(),0.0001,0.0001,\"X\").can_trade)\n    def test_ec_fc_blocks(self):\n        eng = ExposureControlEngine(fail_mode=FailMode.FAIL_CLOSED)\n        with patch.object(eng,'_check_inner',side_effect=RuntimeError):\n            self.assertFalse(eng.check(\"X\",\"BUY\",1.0,[],10_000).can_trade)\n    def test_ec_fo_allows(self):\n        eng = ExposureControlEngine(fail_mode=FailMode.FAIL_OPEN)\n        with patch.object(eng,'_check_inner',side_effect=RuntimeError):\n            self.assertTrue(eng.check(\"X\",\"BUY\",1.0,[],10_000).can_trade)\n    def test_pr_fc_blocks(self):\n        mgr = PortfolioRiskManager(fail_mode=FailMode.FAIL_CLOSED)\n        with patch.object(type(mgr),'_check_inner',side_effect=RuntimeError):\n            self.assertFalse(mgr.check(_trade(),[]).can_trade)\n    def test_pr_fo_allows(self):\n        mgr = PortfolioRiskManager(fail_mode=FailMode.FAIL_OPEN)\n        with patch.object(type(mgr),'_check_inner',side_effect=RuntimeError):\n            self.assertTrue(mgr.check(_trade(),[]).can_trade)\n    def test_ec_exception_logged_fc(self):\n        eng = ExposureControlEngine(fail_mode=FailMode.FAIL_CLOSED)\n        with patch.object(eng,'_check_inner',side_effect=RuntimeError(\"boom\")):\n            with self.assertLogs(\"risk.exposure_control\",level=logging.ERROR):\n                eng.check(\"X\",\"BUY\",1.0,[],10_000)\n    def test_vf_exception_logged(self):\n        vf = VolatilityFilter()\n        with patch.object(vf,'_check_inner',side_effect=RuntimeError(\"boom\")):\n            with self.assertLogs(\"risk.volatility_filter\",level=logging.ERROR):\n                vf.check(0.001,_history(),0.0001,0.0001,\"X\")\n    def test_ec_string_fail_mode(self):\n        self.assertIs(ExposureControlEngine(fail_mode=\"FAIL_OPEN\")._fail_mode, FailMode.FAIL_OPEN)\n\n\n# ======================================================================\n# TOPIC 8 — Portfolio Correlation Calculations (14 tests)\n# ======================================================================\nclass TestPortfolioCorrelationCalcs(unittest.TestCase):\n    \"\"\"\n    Issue: CorrelationFilter had no outer try/except before FIX #6.\n    Patch: check() wraps _check_inner() with try/except + FailMode.\n    Risk: Outer exception + silent allow -> correlated positions -> portfolio crash.\n    Compat: check() signature unchanged; CorrelationFilterConfig compatible.\n    \"\"\"\n    def _engine(self, corr_map):\n        e = MagicMock()\n        async def _gc(a,b): return corr_map.get((a,b),corr_map.get((b,a),0.0))\n        e.get_correlation = _gc; return e\n    def test_no_positions_allowed(self):\n        self.assertTrue(asyncio.run(CorrelationFilter().check(\"EURUSD\",\"BUY\",[],)).can_trade)\n    def test_no_engine_allowed(self):\n        r = asyncio.run(CorrelationFilter().check(\"EURUSD\",\"BUY\",[{\"symbol\":\"GBPUSD\"}]))\n        self.assertTrue(r.can_trade); self.assertIn(\"NO_POSITIONS_OR_ENGINE\", r.reason)\n    def test_high_corr_blocked(self):\n        cf = CorrelationFilter(CorrelationFilterConfig(max_corr=0.85),\n                               self._engine({(\"EURUSD\",\"GBPUSD\"):0.92}))\n        r = asyncio.run(cf.check(\"EURUSD\",\"BUY\",[{\"symbol\":\"GBPUSD\"}]))\n        self.assertFalse(r.can_trade); self.assertIn(\"CORR_TOO_HIGH\",r.reason)\n        self.assertAlmostEqual(r.correlation, 0.92)\n    def test_low_corr_allowed(self):\n        cf = CorrelationFilter(CorrelationFilterConfig(max_corr=0.85),\n                               self._engine({(\"EURUSD\",\"USDJPY\"):0.30}))\n        self.assertTrue(asyncio.run(cf.check(\"EURUSD\",\"BUY\",[{\"symbol\":\"USDJPY\"}])).can_trade)\n    def test_negative_corr_abs_check(self):\n        cf = CorrelationFilter(CorrelationFilterConfig(max_corr=0.85),\n                               self._engine({(\"EURUSD\",\"USDCHF\"):-0.92}))\n        self.assertFalse(asyncio.run(cf.check(\"EURUSD\",\"BUY\",[{\"symbol\":\"USDCHF\"}])).can_trade)\n    def test_corr_at_boundary_blocked(self):\n        cf = CorrelationFilter(CorrelationFilterConfig(max_corr=0.85),\n                               self._engine({(\"EURUSD\",\"GBPUSD\"):0.85}))\n        self.assertFalse(asyncio.run(cf.check(\"EURUSD\",\"BUY\",[{\"symbol\":\"GBPUSD\"}])).can_trade)\n    def test_same_symbol_skipped(self):\n        cf = CorrelationFilter(CorrelationFilterConfig(max_corr=0.85),\n                               self._engine({(\"EURUSD\",\"EURUSD\"):1.0}))\n        self.assertTrue(asyncio.run(cf.check(\"EURUSD\",\"BUY\",[{\"symbol\":\"EURUSD\"}])).can_trade)\n    def test_per_pair_engine_crash_yields_zero(self):\n        e = MagicMock()\n        async def _crash(a,b): raise RuntimeError\n        e.get_correlation = _crash\n        cf = CorrelationFilter(CorrelationFilterConfig(max_corr=0.85), e)\n        self.assertTrue(asyncio.run(cf.check(\"EURUSD\",\"BUY\",[{\"symbol\":\"GBPUSD\"}])).can_trade)\n    def test_fail_closed_outer_blocks(self):\n        cf = CorrelationFilter(fail_mode=FailMode.FAIL_CLOSED)\n        with patch.object(cf,'_check_inner',side_effect=RuntimeError):\n            r = asyncio.run(cf.check(\"EURUSD\",\"BUY\",[{\"symbol\":\"X\"}]))\n        self.assertFalse(r.can_trade); self.assertIn(\"FAIL_CLOSED\",r.reason)\n    def test_fail_open_outer_allows(self):\n        cf = CorrelationFilter(fail_mode=FailMode.FAIL_OPEN)\n        with patch.object(cf,'_check_inner',side_effect=RuntimeError):\n            r = asyncio.run(cf.check(\"EURUSD\",\"BUY\",[{\"symbol\":\"X\"}]))\n        self.assertTrue(r.can_trade); self.assertIn(\"FAIL_OPEN\",r.reason)\n    def test_first_high_corr_triggers_block(self):\n        cf = CorrelationFilter(CorrelationFilterConfig(max_corr=0.85),\n                               self._engine({(\"EURUSD\",\"GBPUSD\"):0.92,(\"EURUSD\",\"AUDUSD\"):0.30}))\n        r = asyncio.run(cf.check(\"EURUSD\",\"BUY\",[{\"symbol\":\"GBPUSD\"},{\"symbol\":\"AUDUSD\"}]))\n        self.assertFalse(r.can_trade); self.assertIn(\"GBPUSD\",r.pair_checked)\n    def test_all_low_corr_allowed(self):\n        cf = CorrelationFilter(CorrelationFilterConfig(max_corr=0.85),\n                               self._engine({(\"EURUSD\",\"GBPUSD\"):0.30,(\"EURUSD\",\"AUDUSD\"):0.25}))\n        self.assertTrue(asyncio.run(cf.check(\"EURUSD\",\"BUY\",[{\"symbol\":\"GBPUSD\"},{\"symbol\":\"AUDUSD\"}])).can_trade)\n    def test_exception_logged_fail_closed(self):\n        cf = CorrelationFilter(fail_mode=FailMode.FAIL_CLOSED)\n        with patch.object(cf,'_check_inner',side_effect=RuntimeError(\"boom\")):\n            with self.assertLogs(\"risk.correlation_filter\",level=logging.CRITICAL):\n                asyncio.run(cf.check(\"EURUSD\",\"BUY\",[{\"symbol\":\"X\"}]))\n    def test_kwarg_fail_mode_overrides_config(self):\n        cfg = CorrelationFilterConfig(fail_mode=FailMode.FAIL_CLOSED)\n        cf  = CorrelationFilter(config=cfg, fail_mode=FailMode.FAIL_OPEN)\n        self.assertIs(cf._fail_mode, FailMode.FAIL_OPEN)\n\n\nif __name__ == \"__main__\":\n    unittest.main(verbosity=2)\n