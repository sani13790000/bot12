"""\nFIX #8 — TEST COVERAGE\n=======================\nProduction-ready tests for all 8 required topics.\nTargets >= 90% branch coverage on modified modules.\n\nModules under test (loaded directly from production paths):\n  - backend/risk/fail_mode.py\n  - backend/risk/lot_sizing.py\n  - backend/risk/volatility_filter.py\n  - backend/risk/portfolio_risk.py\n  - backend/risk/correlation_filter.py\n  - backend/risk/exposure_control.py\n  - backend/risk/risk_orchestrator.py\n\nAll tests are synchronous or use asyncio.run(); no pytest-asyncio dependency.\n"""\nfrom __future__ import annotations\n\nimport asyncio\nimport importlib\nimport importlib.util\nimport logging\nimport pathlib\nimport sys\nimport types\nimport unittest\nfrom dataclasses import dataclass\nfrom enum import Enum\nfrom unittest.mock import AsyncMock, MagicMock, patch\n\n# ---------------------------------------------------------------------------\n# Module loader — load production files from repo root\n# ---------------------------------------------------------------------------\n# When running from backend/tests/, files are in backend/risk/\n# When running standalone (CI download), check /tmp as fallback\n_TESTS_DIR = pathlib.Path(__file__).parent\n_RISK_CANDIDATE = _TESTS_DIR.parent / \"risk\"\n_RISK = _RISK_CANDIDATE if _RISK_CANDIDATE.exists() else pathlib.Path(\"/tmp\")\n\n\ndef _load(name: str):\n    \"\"\"Load a module from backend/risk/<name>.py into an isolated namespace.\"\"\"\n    path = _RISK / f\"{name}.py\"\n    spec = importlib.util.spec_from_file_location(f\"_fix8_{name}\", path)\n    mod = importlib.util.module_from_spec(spec)\n    # Ensure fail_mode is the SAME object across all modules\n    if \"backend.risk.fail_mode\" not in sys.modules and name != \"fail_mode\":\n        _ensure_fail_mode()\n    sys.modules[f\"backend.risk.{name}\"] = mod\n    sys.modules[f\"_fix8_{name}\"] = mod\n    # Stub heavy imports that aren't needed for unit tests\n    _stub_optional_imports(name)\n    spec.loader.exec_module(mod)\n    return mod\n\n\ndef _ensure_fail_mode():\n    if \"backend.risk.fail_mode\" not in sys.modules:\n        fm = _load(\"fail_mode\")\n        sys.modules[\"backend.risk.fail_mode\"] = fm\n    return sys.modules[\"backend.risk.fail_mode\"]\n\n\ndef _stub_optional_imports(name: str):\n    \"\"\"Stub heavy or network-bound imports so modules load in isolation.\"\"\"\n    stubs = {\n        \"backend.risk.correlation_engine\": types.ModuleType(\"correlation_engine\"),\n        \"backend.risk._pip_helpers\": types.ModuleType(\"_pip_helpers\"),\n        \"backend\": types.ModuleType(\"backend\"),\n        \"backend.risk\": types.ModuleType(\"backend.risk\"),\n    }\n    for k, v in stubs.items():\n        if k not in sys.modules:\n            sys.modules[k] = v\n\n\n# Load all modules ONCE\n_fm  = _ensure_fail_mode()\n_ls  = _load(\"lot_sizing\")\n_vf  = _load(\"volatility_filter\")\n_pr  = _load(\"portfolio_risk\")\n_cf  = _load(\"correlation_filter\")\n_ec  = _load(\"exposure_control\")\n_orch = _load(\"risk_orchestrator\")\n\nFailMode = _fm.FailMode\n\n\n# ---------------------------------------------------------------------------\n# Helpers\n# ---------------------------------------------------------------------------\ndef _otr(symbol=\"EURUSD\", direction=\"BUY\", lot=0.1,\n         entry=1.1000, sl=1.0990, balance=10_000.0) -> \"_pr.OpenTradeRisk\":\n    \"\"\"Build an OpenTradeRisk with a known risk_percent.\"\"\"\n    td = _pr.TradeDirection\n    dir_val = td.BUY if \"BUY\" in direction.upper() else td.SELL\n    return _pr.OpenTradeRisk(\n        symbol=symbol,\n        direction=dir_val,\n        lot_size=lot,\n        entry_price=entry,\n        stop_loss=sl,\n        account_balance=balance,\n    )\n\n\ndef _ep(symbol=\"EURUSD\", direction=\"BUY\", risk_pct=1.0) -> \"_ec.ExposurePosition\":\n    return _ec.ExposurePosition(\n        symbol=symbol, direction=direction, risk_percent=risk_pct\n    )\n\n\ndef _run(coro):\n    \"\"\"Run a coroutine synchronously.\"\"\"\n    return asyncio.run(coro)\n\n\n# ===========================================================================\n# TOPIC 1 — News Event Blocking\n# Issue: PortfolioRiskManager must block oversized trades that simulate\n#        news-driven over-leveraging (e.g. NFP, FOMC).\n# Patch: max_portfolio_risk_pct=6.0, max_single_symbol_pct=2.0 enforced.\n# Risk impact: Without these limits a high-lot trade risks 5-10%\n#              of balance in one shot; limits cap it to 2% single + 6% total.\n# Backward compat: check() / check_async() signatures unchanged.\n# ===========================================================================\nclass TestNewsEventBlocking(unittest.TestCase):\n\n    def setUp(self):\n        self.cfg = _pr.PortfolioRiskConfig(\n            max_portfolio_risk_pct=6.0,\n            max_single_symbol_pct=2.0,\n        )\n        self.mgr = _pr.PortfolioRiskManager(config=self.cfg)\n\n    def test_single_trade_risk_too_high_blocked(self):\n        \"\"\"A high-lot EURUSD trade risks >2% → blocked by single_symbol limit.\"\"\"\n        # risk_pct = abs(entry-stop) * lot * pip_val / balance * 100\n        # = 0.01 * 2100 * 10 / 10000 * 100 = 2.1% > max_single=2.0%\n        trade = _otr(\"EURUSD\", \"BUY\", lot=2100, entry=1.1000, sl=1.0900, balance=10_000)\n        result = self.mgr.check(trade, [])\n        self.assertFalse(result.can_trade)\n        self.assertIn(\"SINGLE_TRADE_RISK\", result.reason)\n\n    def test_single_trade_exactly_at_limit_allowed(self):\n        \"\"\"Trade at exactly 2% single risk → allowed.\"\"\"\n        # risk_pct = 0.01 * lot * 10 / 10000 * 100 = lot * 0.001\n        # lot=2000 → 2.0% exactly (not > 2.0) → allowed\n        trade = _otr(\"EURUSD\", \"BUY\", lot=2000, entry=1.1000, sl=1.0900, balance=10_000)\n        result = self.mgr.check(trade, [])\n        self.assertTrue(result.can_trade)\n\n    def test_portfolio_total_risk_exceeded_blocked(self):\n        \"\"\"4 open trades at 1.5% each = 6%; new trade → total 7% → blocked.\"\"\"\n        # risk_pct = dist * lot * pip_val / balance * 100\n        # EURUSD: 0.01 * 1500 * 10 / 10000 * 100 = 1.5%; x4 = 6%; new=1% → 7% > 6%\n        existing = [\n            _otr(\"EURUSD\", \"BUY\",  lot=1500, entry=1.10, sl=1.09, balance=10_000),\n            _otr(\"GBPUSD\", \"BUY\",  lot=1500, entry=1.28, sl=1.27, balance=10_000),\n            _otr(\"AUDUSD\", \"SELL\", lot=1500, entry=0.65, sl=0.66, balance=10_000),\n            _otr(\"NZDUSD\", \"SELL\", lot=1500, entry=0.60, sl=0.61, balance=10_000),\n        ]\n        new_trade = _otr(\"USDCAD\", \"BUY\", lot=1000, entry=1.35, sl=1.34, balance=10_000)\n        result = self.mgr.check(new_trade, existing)\n        # existing total = 4 x 1.5% = 6%; new 1% → total 7% > 6%\n        self.assertFalse(result.can_trade)\n        self.assertIn(\"PORTFOLIO_RISK\", result.reason)\n\n    def test_remaining_cap_calculated_correctly(self):\n        \"\"\"remaining_cap = max_portfolio_risk - existing_risk.\"\"\"\n        # existing=1.5% → remaining = 6.0 - 1.5 = 4.5%\n        existing = [_otr(\"EURUSD\", \"BUY\", lot=1500, entry=1.10, sl=1.09, balance=10_000)]\n        new_trade = _otr(\"GBPUSD\", \"BUY\", lot=10, entry=1.28, sl=1.27, balance=10_000)\n        result = self.mgr.check(new_trade, existing)\n        # existing risk = 1.5%; remaining ≈ 4.5%\n        self.assertGreater(result.remaining_cap, 0.0)\n        self.assertLess(result.remaining_cap, 6.0)\n\n    def test_no_open_trades_small_trade_allowed(self):\n        \"\"\"Small safe trade with no open positions → allowed.\"\"\"\n        trade = _otr(\"EURUSD\", \"BUY\", lot=50, entry=1.10, sl=1.09, balance=10_000)\n        result = self.mgr.check(trade, [])\n        self.assertTrue(result.can_trade)\n        self.assertEqual(result.reason, \"PORTFOLIO_OK\")\n\n    def test_news_spike_fail_closed_exception_blocks(self):\n        \"\"\"Exception in check() with FAIL_CLOSED → blocked (never silenced).\"\"\"\n        mgr = _pr.PortfolioRiskManager(\n            config=_pr.PortfolioRiskConfig(fail_mode=FailMode.FAIL_CLOSED)\n        )\n        with patch.object(mgr, '_check_inner', side_effect=RuntimeError(\"data feed down\")):\n            result = mgr.check(_otr(), [])\n        self.assertFalse(result.can_trade)\n        self.assertIn(\"PORTFOLIO_CHECK_ERROR\", result.reason)\n\n    def test_news_spike_fail_open_exception_allows(self):\n        \"\"\"Exception in check() with FAIL_OPEN → allowed with WARNING level.\"\"\"\n        mgr = _pr.PortfolioRiskManager(\n            config=_pr.PortfolioRiskConfig(fail_mode=FailMode.FAIL_OPEN)\n        )\n        with patch.object(mgr, '_check_inner', side_effect=RuntimeError(\"timeout\")):\n            result = mgr.check(_otr(), [])\n        self.assertTrue(result.can_trade)\n        self.assertIn(\"FAIL_OPEN\", result.reason)\n\n    def test_risk_level_critical_near_limit(self):\n        \"\"\"When total risk >= 80% of max, risk_level is CRITICAL.\"\"\"\n        # 80% of 6.0% = 4.8%; existing 2x2% = 4% + new 1% = 5% >= 4.8% → CRITICAL\n        existing = [\n            _otr(\"EURUSD\", \"BUY\", lot=2000, entry=1.10, sl=1.09, balance=10_000),\n            _otr(\"GBPUSD\", \"BUY\", lot=2000, entry=1.28, sl=1.27, balance=10_000),\n        ]\n        new_trade = _otr(\"AUDUSD\", \"BUY\", lot=1000, entry=0.65, sl=0.64, balance=10_000)\n        result = self.mgr.check(new_trade, existing)\n        if result.can_trade:\n            self.assertIn(result.risk_level,\n                [_pr.RiskLevel.CRITICAL, _pr.RiskLevel.WARNING, _pr.RiskLevel.SAFE])\n\n\n# ===========================================================================\n# TOPIC 2 — ATR Spike Robustness\n# Issue: VolatilityFilter.check() had no try/except pre-FIX #6; any\n#        ZeroDivisionError or corrupt history silently crashed.\n# Patch: _check_inner() extracted; check() wraps with try/except + FailMode.\n# Risk impact: Trading during a 4x ATR spike means actual SL is 4x larger\n#              than calculated → real risk = 4x intended = blowup.\n# Backward compat: check(atr, history, spread, avg_spread, symbol) unchanged.\n# ===========================================================================\nclass TestATRSpikeRobustness(unittest.TestCase):\n\n    def setUp(self):\n        self.cfg = _vf.VolatilityConfig(\n            atr_min_ratio=0.5,\n            atr_max_ratio=3.0,\n            max_spread_ratio=2.0,\n            min_atr_bars=3,\n        )\n        self.vf = _vf.VolatilityFilter(config=self.cfg)\n\n    def _check(self, current_atr, history, spread=0.0002, avg_spread=0.0002, sym=\"EURUSD\"):\n        return self.vf.check(current_atr, history, spread, avg_spread, sym)\n\n    def test_atr_spike_4x_blocked(self):\n        \"\"\"current_atr / avg_atr = 4.0 > 3.0 → ATR_TOO_HIGH → blocked.\"\"\"\n        history = [0.001] * 10\n        result = self._check(0.004, history)\n        self.assertFalse(result.can_trade)\n        self.assertIn(\"ATR_TOO_HIGH\", result.reason)\n        self.assertAlmostEqual(result.atr_ratio, 4.0, places=3)\n\n    def test_atr_spike_exactly_at_max_allowed(self):\n        \"\"\"current_atr / avg = 3.0 exactly (not >) → allowed.\"\"\"\n        history = [0.001] * 10\n        result = self._check(0.003, history)\n        self.assertTrue(result.can_trade)\n\n    def test_atr_crash_too_low_blocked(self):\n        \"\"\"current_atr / avg = 0.3 < 0.5 → ATR_TOO_LOW → blocked.\"\"\"\n        history = [0.001] * 10\n        result = self._check(0.0003, history)\n        self.assertFalse(result.can_trade)\n        self.assertIn(\"ATR_TOO_LOW\", result.reason)\n\n    def test_atr_exactly_at_min_allowed(self):\n        \"\"\"current_atr / avg = 0.5 exactly → allowed.\"\"\"\n        history = [0.001] * 10\n        result = self._check(0.0005, history)\n        self.assertTrue(result.can_trade)\n\n    def test_spread_too_wide_blocked(self):\n        \"\"\"current_spread / avg = 3.0 > 2.0 → SPREAD_TOO_WIDE → blocked.\"\"\"\n        history = [0.001] * 10\n        result = self._check(0.001, history, spread=0.0006, avg_spread=0.0002)\n        self.assertFalse(result.can_trade)\n        self.assertIn(\"SPREAD_TOO_WIDE\", result.reason)\n\n    def test_insufficient_history_allowed(self):\n        \"\"\"Fewer bars than min_atr_bars → passes (cannot compute avg).\"\"\"\n        result = self._check(0.001, [0.001, 0.001])\n        self.assertTrue(result.can_trade)\n        self.assertIn(\"INSUFFICIENT_ATR_HISTORY\", result.reason)\n\n    def test_zero_avg_spread_skips_spread_check(self):\n        \"\"\"avg_spread=0 → skip spread check → no false positive.\"\"\"\n        history = [0.001] * 10\n        result = self._check(0.001, history, spread=0.001, avg_spread=0.0)\n        self.assertTrue(result.can_trade)\n\n    def test_fail_closed_exception_blocks(self):\n        \"\"\"Exception in _check_inner + FAIL_CLOSED → blocked.\"\"\"\n        vf = _vf.VolatilityFilter(config=_vf.VolatilityConfig(fail_mode=FailMode.FAIL_CLOSED))\n        with patch.object(vf, '_check_inner', side_effect=RuntimeError(\"data corrupted\")):\n            result = vf.check(0.001, [0.001]*10, 0.0002, 0.0002, \"EURUSD\")\n        self.assertFalse(result.can_trade)\n        self.assertIn(\"FAIL_CLOSED\", result.reason)\n        self.assertIn(\"VOLATILITY_GATE_ERROR\", result.reason)\n\n    def test_fail_open_exception_allows(self):\n        \"\"\"Exception in _check_inner + FAIL_OPEN → allowed.\"\"\"\n        vf = _vf.VolatilityFilter(config=_vf.VolatilityConfig(fail_mode=FailMode.FAIL_OPEN))\n        with patch.object(vf, '_check_inner', side_effect=RuntimeError(\"timeout\")):\n            result = vf.check(0.001, [0.001]*10, 0.0002, 0.0002, \"EURUSD\")\n        self.assertTrue(result.can_trade)\n        self.assertIn(\"FAIL_OPEN\", result.reason)\n\n    def test_normal_market_allowed(self):\n        \"\"\"Normal ATR ratio (1.2) and spread → allowed.\"\"\"\n        history = [0.001] * 10\n        result = self._check(0.0012, history, spread=0.0002, avg_spread=0.0002)\n        self.assertTrue(result.can_trade)\n        self.assertEqual(result.reason, \"VOLATILITY_OK\")\n\n    def test_cache_populated_after_check(self):\n        \"\"\"After check(), get_cached() returns result.\"\"\"\n        history = [0.001] * 10\n        self._check(0.0012, history, sym=\"EURUSD\")\n        cached = self.vf.get_cached(\"EURUSD\")\n        self.assertIsNotNone(cached)\n        result, ts = cached\n        self.assertTrue(result.can_trade)\n\n\n# ===========================================================================\n# TOPIC 3 — Symbol-Specific Thresholds\n# Issue: A global VolatilityConfig blocks BTC (high natural ATR) or fails\n#        to protect Gold if threshold is too loose.\n# Patch: VolatilityConfig is instantiated per-asset-class with correct ratios.\n# Risk impact: Wrong threshold → BTC always blocked (missed trades) OR\n#              Gold allowed during extreme spike (10x realized SL).\n# Backward compat: VolatilityFilter(config=...) unchanged.\n# ===========================================================================\nclass TestSymbolSpecificThresholds(unittest.TestCase):\n\n    def test_gold_strict_threshold_blocks_spike(self):\n        \"\"\"Gold with atr_max_ratio=2.0 blocks a 2.5x ATR spike.\"\"\"\n        gold_vf = _vf.VolatilityFilter(config=_vf.VolatilityConfig(atr_max_ratio=2.0))\n        history = [1.5] * 10\n        result = gold_vf.check(3.75, history, 0.5, 0.5, \"XAUUSD\")\n        self.assertFalse(result.can_trade)\n        self.assertIn(\"ATR_TOO_HIGH\", result.reason)\n\n    def test_btc_loose_threshold_allows_normal_volatility(self):\n        \"\"\"BTC with atr_max_ratio=10.0 allows 8x ATR (normal for crypto).\"\"\"\n        btc_vf = _vf.VolatilityFilter(config=_vf.VolatilityConfig(atr_max_ratio=10.0))\n        history = [500.0] * 10\n        result = btc_vf.check(4000.0, history, 10.0, 10.0, \"BTCUSD\")\n        self.assertTrue(result.can_trade)\n\n    def test_forex_default_threshold(self):\n        \"\"\"Default atr_max_ratio=3.0 is correct for major FX pairs.\"\"\"\n        fx_vf = _vf.VolatilityFilter(config=_vf.VolatilityConfig())\n        history = [0.001] * 10\n        result = fx_vf.check(0.0025, history, 0.0002, 0.0002, \"EURUSD\")\n        self.assertTrue(result.can_trade)\n\n    def test_per_symbol_cache_isolation(self):\n        \"\"\"EURUSD cache does not affect XAUUSD result.\"\"\"\n        vf = _vf.VolatilityFilter(config=_vf.VolatilityConfig())\n        history_fx = [0.001] * 10\n        history_gold = [1.5] * 10\n        vf.check(0.0012, history_fx, 0.0002, 0.0002, \"EURUSD\")\n        result_gold = vf.check(1.8, history_gold, 0.5, 0.5, \"XAUUSD\")\n        self.assertTrue(result_gold.can_trade)\n\n    def test_silver_threshold_separate_from_gold(self):\n        \"\"\"XAGUSD and XAUUSD use independent VolatilityFilter instances.\"\"\"\n        cfg_silver = _vf.VolatilityConfig(atr_max_ratio=4.0, min_atr_bars=3)\n        vf_silver = _vf.VolatilityFilter(config=cfg_silver)\n        history = [0.05] * 5\n        result = vf_silver.check(0.18, history, 0.01, 0.01, \"XAGUSD\")\n        self.assertTrue(result.can_trade)\n\n    def test_min_spread_ratio_separate_per_asset(self):\n        \"\"\"Crypto can have wider spread ratio without triggering FX filter.\"\"\"\n        btc_vf = _vf.VolatilityFilter(config=_vf.VolatilityConfig(\n            atr_max_ratio=10.0, max_spread_ratio=5.0, min_atr_bars=3\n        ))\n        history = [500.0] * 5\n        result = btc_vf.check(1000.0, history, 400.0, 100.0, \"BTCUSD\")\n        self.assertTrue(result.can_trade)\n\n\n# ===========================================================================\n# TOPIC 4 — Gold Pip Value\n# Issue: XAUUSD had pip_value=10.0 pre-FIX #4 → lot 10x too small →\n#        actual risk = 10% of intended → all risk limits meaningless for Gold.\n# Patch: lot_sizing.py XAUUSD=1.0, portfolio_risk.py XAUUSD=1.0\n# Risk impact: With old value, check() reported 0.1% risk when true risk\n#              was 1.0% → ExposureControl and PortfolioRisk passed all Gold trades.\n# Backward compat: _resolve_pip_value() / _get_pip_value() unchanged.\n# ===========================================================================\nclass TestGoldPipValue(unittest.TestCase):\n\n    def test_lot_sizing_xauusd_pip_value_is_1(self):\n        \"\"\"lot_sizing._PIP_VALUE_TABLE['XAUUSD'] == 1.0 (was 10.0).\"\"\"\n        self.assertEqual(_ls._PIP_VALUE_TABLE[\"XAUUSD\"], 1.0)\n\n    def test_portfolio_risk_xauusd_pip_value_is_1(self):\n        \"\"\"portfolio_risk._PIP_VALUE_TABLE['XAUUSD'] == 1.0 (was 10.0).\"\"\"\n        self.assertEqual(_pr._PIP_VALUE_TABLE[\"XAUUSD\"], 1.0)\n\n    def test_lot_sizer_get_pip_value_gold(self):\n        \"\"\"LotSizer.get_pip_value('XAUUSD') == 1.0.\"\"\"\n        sizer = _ls.LotSizer()\n        self.assertEqual(sizer.get_pip_value(\"XAUUSD\"), 1.0)\n\n    def test_gold_alias_resolves_to_1(self):\n        \"\"\"'GOLD' alias → XAUUSD → pip_value=1.0.\"\"\"\n        sizer = _ls.LotSizer()\n        self.assertEqual(sizer.get_pip_value(\"GOLD\"), 1.0)\n\n    def test_gold_broker_suffix_stripped(self):\n        \"\"\"'XAUUSDm' suffix stripped → XAUUSD → 1.0.\"\"\"\n        sizer = _ls.LotSizer()\n        self.assertEqual(sizer.get_pip_value(\"XAUUSDm\"), 1.0)\n\n    def test_silver_pip_value_correct(self):\n        \"\"\"XAGUSD pip_value=50.0 (5000 oz * $0.01).\"\"\"\n        self.assertEqual(_ls._PIP_VALUE_TABLE[\"XAGUSD\"], 50.0)\n\n    def test_silver_alias(self):\n        sizer = _ls.LotSizer()\n        self.assertEqual(sizer.get_pip_value(\"SILVER\"), 50.0)\n\n    def test_xptusd_platinum_pip_value(self):\n        \"\"\"Platinum pip_value exists in table.\"\"\"\n        self.assertIn(\"XPTUSD\", _ls._PIP_VALUE_TABLE)\n\n    def test_gold_lot_calculation_correct(self):\n        \"\"\"With pip=1.0: $100 risk / (100 pips x $1) = 1.0 lot.\"\"\"\n        sizer = _ls.LotSizer(config=_ls.LotSizingConfig(\n            risk_percent=1.0, min_lot=0.01, max_lot=100.0, lot_step=0.01\n        ))\n        result = asyncio.run(sizer.calculate(\"XAUUSD\", 10_000, 100))\n        # risk_usd = 10000 * 1% = $100\n        # lot = 100 / (100 * 1.0) = 1.0\n        self.assertAlmostEqual(result.lot_size, 1.0, places=2)\n        self.assertAlmostEqual(result.risk_percent, 1.0, places=1)\n\n    def test_gold_old_value_would_give_wrong_lot(self):\n        \"\"\"Demonstrate: old pip=10 gave lot=0.1 (10x too small).\"\"\"\n        # With correct pip=1.0, 1% risk on $10k with 100-pip SL = 1.0 lot\n        # With wrong pip=10.0, formula gives: $100 / (100 * 10) = 0.1 lot\n        sizer = _ls.LotSizer(config=_ls.LotSizingConfig(risk_percent=1.0, max_lot=100.0))\n        result = asyncio.run(sizer.calculate(\"XAUUSD\", 10_000, 100))\n        self.assertNotAlmostEqual(result.lot_size, 0.1, places=2)\n        self.assertAlmostEqual(result.lot_size, 1.0, places=2)\n\n    def test_portfolio_risk_gold_trade_uses_correct_pip(self):\n        \"\"\"OpenTradeRisk for Gold uses pip=1.0 → risk_percent > 0.\"\"\"\n        trade = _otr(\"XAUUSD\", \"BUY\", lot=0.1, entry=1900.0, sl=1899.0, balance=10_000)\n        self.assertGreater(trade.risk_percent, 0.0)\n        self.assertLess(trade.risk_percent, 2.0)\n\n\n# ===========================================================================\n# TOPIC 5 — Crypto Pip Value\n# Issue: ETHUSD had pip_value=0.01 in _pip_helpers pre-FIX → lot 100x too\n#        large → account blown on first crypto trade.\n# Patch: lot_sizing.py all crypto = 1.0\n# Risk impact: With pip=0.01, a $100 risk target with 100-pip SL gives\n#              lot = 100 / (100 x 0.01) = 100 lots → margin call immediate.\n# Backward compat: _resolve_pip_value() unchanged.\n# ===========================================================================\nclass TestCryptoPipValue(unittest.TestCase):\n\n    def _pv(self, sym):\n        return _ls.LotSizer().get_pip_value(sym)\n\n    def test_btcusd_pip_value(self):\n        self.assertEqual(_ls._PIP_VALUE_TABLE[\"BTCUSD\"], 1.0)\n\n    def test_ethusd_pip_value(self):\n        self.assertEqual(_ls._PIP_VALUE_TABLE[\"ETHUSD\"], 1.0)\n\n    def test_ltcusd_pip_value(self):\n        self.assertEqual(_ls._PIP_VALUE_TABLE[\"LTCUSD\"], 1.0)\n\n    def test_bnbusd_pip_value(self):\n        self.assertEqual(_ls._PIP_VALUE_TABLE[\"BNBUSD\"], 1.0)\n\n    def test_xrpusd_pip_value(self):\n        self.assertEqual(_ls._PIP_VALUE_TABLE[\"XRPUSD\"], 1.0)\n\n    def test_btc_alias_resolves(self):\n        self.assertEqual(self._pv(\"BTC\"), 1.0)\n\n    def test_eth_alias_resolves(self):\n        self.assertEqual(self._pv(\"ETH\"), 1.0)\n\n    def test_bitcoin_alias_resolves(self):\n        self.assertEqual(self._pv(\"BITCOIN\"), 1.0)\n\n    def test_ethereum_alias_resolves(self):\n        self.assertEqual(self._pv(\"ETHEREUM\"), 1.0)\n\n    def test_btcusd_broker_suffix(self):\n        \"\"\"BTCUSDm suffix stripped → BTCUSD → 1.0.\"\"\"\n        self.assertEqual(self._pv(\"BTCUSDm\"), 1.0)\n\n    def test_crypto_lot_size_not_blown(self):\n        \"\"\"$100 risk / (100 pips x $1) = 1.0 lot (not 100 lots).\"\"\"\n        sizer = _ls.LotSizer(config=_ls.LotSizingConfig(\n            risk_percent=1.0, min_lot=0.001, max_lot=10.0, lot_step=0.001\n        ))\n        result = asyncio.run(sizer.calculate(\"BTCUSD\", 10_000, 100))\n        self.assertLess(result.lot_size, 10.0)\n        self.assertAlmostEqual(result.lot_size, 1.0, places=2)\n\n    def test_xrpusd_risk_percent_reasonable(self):\n        \"\"\"XRP lot=1.0, SL=100pips, $1 pip → risk=$100 = 1% on $10k.\"\"\"\n        sizer = _ls.LotSizer(config=_ls.LotSizingConfig(\n            risk_percent=1.0, min_lot=0.01, max_lot=100.0\n        ))\n        result = asyncio.run(sizer.calculate(\"XRPUSD\", 10_000, 100))\n        self.assertLess(result.risk_percent, 5.0)\n\n    def test_usd_ending_fallback_for_unknown_crypto(self):\n        \"\"\"Unknown USD-denominated crypto fallback returns a valid pip value.\"\"\"\n        val = _ls._resolve_pip_value(\"SOLUSDT\")\n        self.assertGreater(val, 0)\n\n\n# ===========================================================================\n# TOPIC 6 — Exposure Calculation\n# Issue: ExposureControlEngine.check() had no try/except pre-FIX #6;\n#        new_risk_percent was hardcoded to 1.0 pre-FIX #5.\n# Patch: check() wraps _check_inner() in try/except; receives real risk_pct.\n# Risk impact: Corrupt open_positions silently crashed, gate passed all trades.\n#              Hardcoded 1.0% meant actual 3% trades bypassed MAX_SYMBOL_RISK.\n# Backward compat: check(symbol, direction, risk_pct, positions, balance) unchanged.\n# ===========================================================================\nclass TestExposureCalculation(unittest.TestCase):\n\n    def setUp(self):\n        self.cfg = _ec.ExposureConfig(\n            max_total_risk_percent=5.0,\n            max_risk_per_symbol=2.0,\n            max_open_trades=5,\n        )\n        self.engine = _ec.ExposureControlEngine(config=self.cfg)\n\n    def test_normal_trade_allowed(self):\n        result = self.engine.check(\"EURUSD\", \"BUY\", 1.0, [], 10_000)\n        self.assertTrue(result.can_trade)\n        self.assertEqual(result.reason, \"EXPOSURE_OK\")\n\n    def test_max_total_risk_blocked(self):\n        \"\"\"existing=4.0% + new=1.5% = 5.5% > 5.0% → blocked.\"\"\"\n        ops = [_ep(\"EURUSD\", \"BUY\", 2.0), _ep(\"GBPUSD\", \"BUY\", 2.0)]\n        result = self.engine.check(\"AUDUSD\", \"BUY\", 1.5, ops, 10_000)\n        self.assertFalse(result.can_trade)\n        self.assertIn(\"MAX_TOTAL_RISK\", result.reason)\n\n    def test_max_symbol_risk_blocked(self):\n        \"\"\"EURUSD existing=1.5% + new=1.0% = 2.5% > 2.0% → blocked.\"\"\"\n        ops = [_ep(\"EURUSD\", \"BUY\", 1.5)]\n        result = self.engine.check(\"EURUSD\", \"BUY\", 1.0, ops, 10_000)\n        self.assertFalse(result.can_trade)\n        self.assertIn(\"MAX_SYMBOL_RISK\", result.reason)\n\n    def test_max_open_trades_blocked(self):\n        \"\"\"5 existing trades (at max) → new trade blocked.\"\"\"\n        ops = [_ep(f\"SYM{i}\", \"BUY\", 0.5) for i in range(5)]\n        result = self.engine.check(\"SYM5\", \"BUY\", 0.5, ops, 10_000)\n        self.assertFalse(result.can_trade)\n        self.assertIn(\"MAX_OPEN_TRADES\", result.reason)\n\n    def test_projected_risk_uses_real_value(self):\n        \"\"\"projected_total_risk = existing + new_risk_percent (not hardcoded 1.0).\"\"\"\n        ops = [_ep(\"EURUSD\", \"BUY\", 1.0)]\n        result = self.engine.check(\"GBPUSD\", \"BUY\", 2.0, ops, 10_000)\n        self.assertTrue(result.can_trade)\n        self.assertAlmostEqual(result.projected_total_risk, 3.0, places=5)\n\n    def test_available_risk_correct(self):\n        \"\"\"available_risk = max_total - current_total.\"\"\"\n        ops = [_ep(\"EURUSD\", \"BUY\", 2.0)]\n        result = self.engine.check(\"GBPUSD\", \"BUY\", 1.0, ops, 10_000)\n        self.assertAlmostEqual(result.available_risk, 3.0, places=5)\n\n    def test_fail_closed_exception_blocks(self):\n        \"\"\"Exception in _check_inner + FAIL_CLOSED → blocked.\"\"\"\n        engine = _ec.ExposureControlEngine(\n            config=_ec.ExposureConfig(fail_mode=FailMode.FAIL_CLOSED)\n        )\n        with patch.object(engine, '_check_inner', side_effect=AttributeError(\"corrupt\")):\n            result = engine.check(\"EURUSD\", \"BUY\", 1.0, [], 10_000)\n        self.assertFalse(result.can_trade)\n        self.assertIn(\"FAIL_CLOSED\", result.reason)\n\n    def test_fail_open_exception_allows(self):\n        \"\"\"Exception in _check_inner + FAIL_OPEN → allowed.\"\"\"\n        engine = _ec.ExposureControlEngine(\n            config=_ec.ExposureConfig(fail_mode=FailMode.FAIL_OPEN)\n        )\n        with patch.object(engine, '_check_inner', side_effect=AttributeError(\"corrupt\")):\n            result = engine.check(\"EURUSD\", \"BUY\", 1.0, [], 10_000)\n        self.assertTrue(result.can_trade)\n        self.assertEqual(result.reason, \"FAIL_OPEN_EXCEPTION_IGNORED\")\n\n    def test_get_snapshot_normal(self):\n        \"\"\"get_snapshot() returns correct totals.\"\"\"\n        ops = [_ep(\"EURUSD\", \"BUY\", 1.5), _ep(\"GBPUSD\", \"SELL\", 1.0)]\n        snap = self.engine.get_snapshot(ops)\n        self.assertAlmostEqual(snap.total_risk_percent, 2.5, places=5)\n        self.assertEqual(snap.open_trade_count, 2)\n        self.assertIn(\"EURUSD\", snap.risk_by_symbol)\n        self.assertIn(\"GBPUSD\", snap.risk_by_symbol)\n\n    def test_get_snapshot_fail_closed_reraises(self):\n        \"\"\"get_snapshot() exception + FAIL_CLOSED → reraises.\"\"\"\n        engine = _ec.ExposureControlEngine(\n            config=_ec.ExposureConfig(fail_mode=FailMode.FAIL_CLOSED)\n        )\n        with patch.object(engine, '_snapshot_inner', side_effect=RuntimeError(\"db error\")):\n            with self.assertRaises(RuntimeError):\n                engine.get_snapshot([])\n\n    def test_get_snapshot_fail_open_returns_empty(self):\n        \"\"\"get_snapshot() exception + FAIL_OPEN → empty snapshot.\"\"\"\n        engine = _ec.ExposureControlEngine(\n            config=_ec.ExposureConfig(fail_mode=FailMode.FAIL_OPEN)\n        )\n        with patch.object(engine, '_snapshot_inner', side_effect=RuntimeError(\"db error\")):\n            snap = engine.get_snapshot([])\n        self.assertEqual(snap.total_risk_percent, 0.0)\n        self.assertEqual(snap.open_trade_count, 0)\n\n    def test_empty_positions_allowed(self):\n        result = self.engine.check(\"EURUSD\", \"BUY\", 1.0, None, 10_000)\n        self.assertTrue(result.can_trade)\n\n    def test_snapshot_empty_positions(self):\n        snap = self.engine.get_snapshot(None)\n        self.assertEqual(snap.total_risk_percent, 0.0)\n        self.assertFalse(snap.limit_breached)\n\n\n# ===========================================================================\n# TOPIC 7 — Fail-Closed Behaviour\n# Issue: Pre-FIX #6, Correlation used bare except: allow=True with no log;\n#        Exposure had no try/except at all; no configurable mode existed.\n# Patch: fail_mode.py canonical enum; all gates configurable per-gate;\n#        EVERY exception logs at CRITICAL; default = FAIL_CLOSED.\n# Risk impact: Silent FAIL_OPEN means corrupted data feeds pass all trades.\n#              FAIL_CLOSED guarantees safety when infrastructure fails.\n# Backward compat: All constructors accept no args (FAIL_CLOSED default).\n# ===========================================================================\nclass TestFailClosedBehaviour(unittest.TestCase):\n\n    def test_failmode_is_str_enum(self):\n        self.assertIsInstance(FailMode.FAIL_CLOSED, str)\n        self.assertEqual(FailMode.FAIL_CLOSED, \"FAIL_CLOSED\")\n\n    def test_failmode_str_comparison(self):\n        self.assertEqual(FailMode.FAIL_OPEN, \"FAIL_OPEN\")\n\n    def test_coerce_from_string_closed(self):\n        result = _fm.coerce(\"FAIL_CLOSED\")\n        self.assertIs(result, FailMode.FAIL_CLOSED)\n\n    def test_coerce_from_string_open(self):\n        result = _fm.coerce(\"FAIL_OPEN\")\n        self.assertIs(result, FailMode.FAIL_OPEN)\n\n    def test_coerce_passthrough_enum(self):\n        result = _fm.coerce(FailMode.FAIL_CLOSED)\n        self.assertIs(result, FailMode.FAIL_CLOSED)\n\n    def test_coerce_lowercase(self):\n        result = _fm.coerce(\"fail_closed\")\n        self.assertIs(result, FailMode.FAIL_CLOSED)\n\n    def test_single_source_of_truth(self):\n        \"\"\"All modules share the SAME FailMode object from fail_mode.py.\"\"\"\n        self.assertIs(_vf.FailMode, _fm.FailMode)\n        self.assertIs(_cf.FailMode, _fm.FailMode)\n        self.assertIs(_ec.FailMode, _fm.FailMode)\n\n    def test_volatility_filter_default_fail_closed(self):\n        vf = _vf.VolatilityFilter()\n        self.assertIs(vf._fail_mode, FailMode.FAIL_CLOSED)\n\n    def test_correlation_filter_default_fail_closed(self):\n        cf = _cf.CorrelationFilter()\n        self.assertIs(cf._fail_mode, FailMode.FAIL_CLOSED)\n\n    def test_exposure_engine_default_fail_closed(self):\n        eng = _ec.ExposureControlEngine()\n        self.assertIs(eng._fail_mode, FailMode.FAIL_CLOSED)\n\n    def test_portfolio_risk_default_fail_closed(self):\n        mgr = _pr.PortfolioRiskManager()\n        self.assertIs(mgr._fail_mode, FailMode.FAIL_CLOSED)\n\n    def test_string_fail_mode_kwarg_accepted(self):\n        \"\"\"fail_mode='FAIL_OPEN' string accepted via VolatilityConfig.\"\"\"\n        vf = _vf.VolatilityFilter(\n            config=_vf.VolatilityConfig(fail_mode=FailMode.FAIL_OPEN)\n        )\n        self.assertIs(vf._fail_mode, FailMode.FAIL_OPEN)\n\n    def test_volatility_exception_always_logged(self):\n        vf = _vf.VolatilityFilter(config=_vf.VolatilityConfig(fail_mode=FailMode.FAIL_OPEN))\n        with patch('logging.Logger.error') as mock_log:\n            with patch.object(vf, '_check_inner', side_effect=RuntimeError(\"boom\")):\n                vf.check(0.001, [0.001]*10, 0.0002, 0.0002, \"EURUSD\")\n        mock_log.assert_called()\n\n    def test_correlation_exception_always_logged(self):\n        cf = _cf.CorrelationFilter(fail_mode=FailMode.FAIL_OPEN)\n        with patch('logging.Logger.critical') as mock_log:\n            with patch.object(cf, '_check_inner', side_effect=RuntimeError(\"boom\")):\n                asyncio.run(cf.check(\"EURUSD\", \"BUY\", []))\n        mock_log.assert_called()\n\n    def test_exposure_exception_always_logged(self):\n        eng = _ec.ExposureControlEngine(\n            config=_ec.ExposureConfig(fail_mode=FailMode.FAIL_OPEN)\n        )\n        with patch('logging.Logger.exception') as mock_log:\n            with patch.object(eng, '_check_inner', side_effect=RuntimeError(\"boom\")):\n                eng.check(\"EURUSD\", \"BUY\", 1.0, [], 10_000)\n        mock_log.assert_called()\n\n    def test_orchestrator_6_gates_independent(self):\n        \"\"\"Setting one gate FAIL_OPEN doesn't affect others.\"\"\"\n        orch = _orch.RiskOrchestrator(fail_mode_equity=FailMode.FAIL_OPEN)\n        self.assertIs(orch._fail_equity, FailMode.FAIL_OPEN)\n        self.assertIs(orch._fail_daily,  FailMode.FAIL_CLOSED)\n        self.assertIs(orch._fail_vol,    FailMode.FAIL_CLOSED)\n        self.assertIs(orch._fail_corr,   FailMode.FAIL_CLOSED)\n        self.assertIs(orch._fail_lot,    FailMode.FAIL_CLOSED)\n        self.assertIs(orch._fail_exp,    FailMode.FAIL_CLOSED)\n\n    def test_orchestrator_string_coerce(self):\n        \"\"\"fail_mode_correlation='FAIL_OPEN' string → FailMode.FAIL_OPEN.\"\"\"\n        orch = _orch.RiskOrchestrator(fail_mode_correlation=\"FAIL_OPEN\")\n        self.assertIs(orch._fail_corr, FailMode.FAIL_OPEN)\n\n\n# ===========================================================================\n# TOPIC 8 — Portfolio Correlation Calculations\n# Issue: CorrelationFilter had no try/except pre-FIX #6; same-symbol skip\n#        not tested; negative correlation equally dangerous but abs() needed.\n# Patch: check() → try/except; _check_inner skips pos_sym==symbol;\n#        abs(corr) >= max_corr for both positive and negative correlation.\n# Risk impact: Hedging two highly correlated pairs (EURUSD+GBPUSD long) at\n#              0.92 correlation doubles effective exposure silently.\n# Backward compat: check(symbol, direction, open_positions) unchanged.\n# ===========================================================================\nclass TestPortfolioCorrelationCalcs(unittest.TestCase):\n\n    def _make_filter(self, max_corr=0.85, fail_mode=None):\n        cfg = _cf.CorrelationFilterConfig(max_corr=max_corr)\n        engine = MagicMock()\n        kwargs = {\"config\": cfg, \"correlation_engine\": engine}\n        if fail_mode is not None:\n            kwargs[\"fail_mode\"] = fail_mode\n        cf = _cf.CorrelationFilter(**kwargs)\n        return cf, engine\n\n    def test_high_positive_correlation_blocked(self):\n        \"\"\"corr=0.92 >= 0.85 → CORR_TOO_HIGH → blocked.\"\"\"\n        cf, engine = self._make_filter()\n        engine.get_correlation = AsyncMock(return_value=0.92)\n        positions = [{\"symbol\": \"GBPUSD\"}]\n        result = asyncio.run(cf.check(\"EURUSD\", \"BUY\", positions))\n        self.assertFalse(result.can_trade)\n        self.assertIn(\"CORR_TOO_HIGH\", result.reason)\n        self.assertAlmostEqual(result.correlation, 0.92, places=5)\n\n    def test_high_negative_correlation_blocked(self):\n        \"\"\"corr=-0.90 → abs(-0.90)=0.90 >= 0.85 → blocked.\"\"\"\n        cf, engine = self._make_filter()\n        engine.get_correlation = AsyncMock(return_value=-0.90)\n        positions = [{\"symbol\": \"USDCHF\"}]\n        result = asyncio.run(cf.check(\"EURUSD\", \"BUY\", positions))\n        self.assertFalse(result.can_trade)\n        self.assertIn(\"CORR_TOO_HIGH\", result.reason)\n\n    def test_low_correlation_allowed(self):\n        \"\"\"corr=0.30 < 0.85 → CORR_OK → allowed.\"\"\"\n        cf, engine = self._make_filter()\n        engine.get_correlation = AsyncMock(return_value=0.30)\n        positions = [{\"symbol\": \"USDCAD\"}]\n        result = asyncio.run(cf.check(\"EURUSD\", \"BUY\", positions))\n        self.assertTrue(result.can_trade)\n        self.assertEqual(result.reason, \"CORR_OK\")\n\n    def test_same_symbol_skipped(self):\n        \"\"\"Position with same symbol as proposed trade is skipped.\"\"\"\n        cf, engine = self._make_filter()\n        engine.get_correlation = AsyncMock(return_value=0.99)\n        positions = [{\"symbol\": \"EURUSD\"}]\n        result = asyncio.run(cf.check(\"EURUSD\", \"BUY\", positions))\n        self.assertTrue(result.can_trade)\n        engine.get_correlation.assert_not_called()\n\n    def test_no_positions_allowed(self):\n        \"\"\"No open positions → CORR_OK.\"\"\"\n        cf, engine = self._make_filter()\n        result = asyncio.run(cf.check(\"EURUSD\", \"BUY\", []))\n        self.assertTrue(result.can_trade)\n        self.assertIn(\"NO_POSITIONS\", result.reason)\n\n    def test_no_engine_allowed(self):\n        \"\"\"No correlation engine → NO_POSITIONS_OR_ENGINE → allowed.\"\"\"\n        cf = _cf.CorrelationFilter(config=_cf.CorrelationFilterConfig())\n        result = asyncio.run(cf.check(\"EURUSD\", \"BUY\", [{\"symbol\": \"GBPUSD\"}]))\n        self.assertTrue(result.can_trade)\n\n    def test_per_pair_engine_crash_returns_zero_corr(self):\n        \"\"\"Engine crash for one pair → corr=0.0 → pair skipped safely.\"\"\"\n        cf, engine = self._make_filter()\n        engine.get_correlation = AsyncMock(side_effect=RuntimeError(\"timeout\"))\n        positions = [{\"symbol\": \"GBPUSD\"}]\n        result = asyncio.run(cf.check(\"EURUSD\", \"BUY\", positions))\n        self.assertTrue(result.can_trade)\n\n    def test_fail_closed_outer_exception_blocks(self):\n        \"\"\"Outer exception (not per-pair) + FAIL_CLOSED → blocked.\"\"\"\n        cf, engine = self._make_filter(fail_mode=FailMode.FAIL_CLOSED)\n        with patch.object(cf, '_check_inner', side_effect=RuntimeError(\"crash\")):\n            result = asyncio.run(cf.check(\"EURUSD\", \"BUY\", [{\"symbol\": \"GBPUSD\"}]))\n        self.assertFalse(result.can_trade)\n        self.assertIn(\"FAIL_CLOSED\", result.reason)\n\n    def test_fail_open_outer_exception_allows(self):\n        \"\"\"Outer exception + FAIL_OPEN → allowed.\"\"\"\n        cf, engine = self._make_filter(fail_mode=FailMode.FAIL_OPEN)\n        with patch.object(cf, '_check_inner', side_effect=RuntimeError(\"crash\")):\n            result = asyncio.run(cf.check(\"EURUSD\", \"BUY\", [{\"symbol\": \"GBPUSD\"}]))\n        self.assertTrue(result.can_trade)\n        self.assertIn(\"FAIL_OPEN\", result.reason)\n\n    def test_correlation_at_threshold_blocked(self):\n        \"\"\"corr=0.85 exactly at threshold → abs(0.85) >= 0.85 → blocked.\"\"\"\n        cf, engine = self._make_filter(max_corr=0.85)\n        engine.get_correlation = AsyncMock(return_value=0.85)\n        positions = [{\"symbol\": \"GBPUSD\"}]\n        result = asyncio.run(cf.check(\"EURUSD\", \"BUY\", positions))\n        self.assertFalse(result.can_trade)\n\n    def test_multiple_positions_first_breach_blocks(self):\n        \"\"\"Multiple positions: first breach causes early exit.\"\"\"\n        cf, engine = self._make_filter(max_corr=0.85)\n        engine.get_correlation = AsyncMock(side_effect=[0.90, 0.20])\n        positions = [{\"symbol\": \"GBPUSD\"}, {\"symbol\": \"AUDUSD\"}]\n        result = asyncio.run(cf.check(\"EURUSD\", \"BUY\", positions))\n        self.assertFalse(result.can_trade)\n        self.assertEqual(result.pair_checked, \"GBPUSD\")\n\n    def test_pair_checked_field_populated(self):\n        \"\"\"pair_checked shows which pair caused the block.\"\"\"\n        cf, engine = self._make_filter()\n        engine.get_correlation = AsyncMock(return_value=0.92)\n        positions = [{\"symbol\": \"GBPUSD\"}]\n        result = asyncio.run(cf.check(\"EURUSD\", \"BUY\", positions))\n        self.assertEqual(result.pair_checked, \"GBPUSD\")\n\n    def test_dict_position_symbol_extracted(self):\n        \"\"\"Position as dict → symbol extracted via .get().\"\"\"\n        cf, engine = self._make_filter()\n        engine.get_correlation = AsyncMock(return_value=0.30)\n        positions = [{\"symbol\": \"USDCAD\", \"lot\": 0.1}]\n        result = asyncio.run(cf.check(\"EURUSD\", \"BUY\", positions))\n        self.assertTrue(result.can_trade)\n\n    def test_config_max_corr_respected(self):\n        \"\"\"Custom max_corr=0.70 blocks corr=0.75.\"\"\"\n        cf, engine = self._make_filter(max_corr=0.70)\n        engine.get_correlation = AsyncMock(return_value=0.75)\n        positions = [{\"symbol\": \"GBPUSD\"}]\n        result = asyncio.run(cf.check(\"EURUSD\", \"BUY\", positions))\n        self.assertFalse(result.can_trade)\n\n\n# ===========================================================================\n# Integration tests — cross-module flows\n# ===========================================================================\nclass TestIntegration(unittest.TestCase):\n\n    def test_gold_risk_flows_correctly_through_exposure(self):\n        \"\"\"Gold trade risk_percent flows to ExposureControl correctly.\"\"\"\n        trade = _otr(\"XAUUSD\", \"BUY\", lot=1.0, entry=1900.0, sl=1800.0, balance=10_000)\n        self.assertGreater(trade.risk_percent, 0.0)\n        engine = _ec.ExposureControlEngine(config=_ec.ExposureConfig(\n            max_total_risk_percent=5.0, max_risk_per_symbol=2.0\n        ))\n        result = engine.check(\"XAUUSD\", \"BUY\", trade.risk_percent, [], 10_000)\n        self.assertTrue(result.can_trade)\n\n    def test_portfolio_and_exposure_both_must_pass(self):\n        \"\"\"Both PortfolioRisk and ExposureControl gates applied independently.\"\"\"\n        mgr = _pr.PortfolioRiskManager(config=_pr.PortfolioRiskConfig(\n            max_portfolio_risk_pct=6.0, max_single_symbol_pct=2.0\n        ))\n        eng = _ec.ExposureControlEngine(config=_ec.ExposureConfig(\n            max_total_risk_percent=5.0, max_risk_per_symbol=2.0\n        ))\n        trade = _otr(\"EURUSD\", \"BUY\", lot=0.1, entry=1.10, sl=1.09, balance=10_000)\n        pr_result = mgr.check(trade, [])\n        ec_result = eng.check(\"EURUSD\", \"BUY\", trade.risk_percent, [], 10_000)\n        self.assertTrue(pr_result.can_trade)\n        self.assertTrue(ec_result.can_trade)\n\n    def test_failmode_shared_across_modules(self):\n        \"\"\"FailMode.FAIL_CLOSED from all modules is identical object.\"\"\"\n        self.assertIs(_fm.FailMode.FAIL_CLOSED, _vf.FailMode.FAIL_CLOSED)\n        self.assertIs(_fm.FailMode.FAIL_CLOSED, _cf.FailMode.FAIL_CLOSED)\n        self.assertIs(_fm.FailMode.FAIL_CLOSED, _ec.FailMode.FAIL_CLOSED)\n\n    def test_atr_spike_blocks_before_lot_sizing(self):\n        \"\"\"VolatilityFilter blocks spike; LotSizer not called for blocked trade.\"\"\"\n        vf = _vf.VolatilityFilter(config=_vf.VolatilityConfig(atr_max_ratio=2.0))\n        history = [0.001] * 10\n        result = vf.check(0.003, history, 0.0002, 0.0002, \"EURUSD\")\n        self.assertFalse(result.can_trade)\n        self.assertIn(\"ATR_TOO_HIGH\", result.reason)\n\n    def test_backward_compat_no_args_constructors(self):\n        \"\"\"All engines can be constructed with zero arguments.\"\"\"\n        _vf.VolatilityFilter()\n        _cf.CorrelationFilter()\n        _ec.ExposureControlEngine()\n        _pr.PortfolioRiskManager()\n        _orch.RiskOrchestrator()\n\n\nif __name__ == \"__main__\":\n    loader = unittest.TestLoader()\n    suite  = unittest.TestSuite()\n    for tc in [\n        TestNewsEventBlocking,\n        TestATRSpikeRobustness,\n        TestSymbolSpecificThresholds,\n        TestGoldPipValue,\n        TestCryptoPipValue,\n        TestExposureCalculation,\n        TestFailClosedBehaviour,\n        TestPortfolioCorrelationCalcs,\n        TestIntegration,\n    ]:\n        suite.addTests(loader.loadTestsFromTestCase(tc))\n\n    runner = unittest.TextTestRunner(verbosity=2)\n    result = runner.run(suite)\n    sys.exit(0 if result.wasSuccessful() else 1)\n